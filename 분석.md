# ABCchallenges 프로젝트 구조 분석 및 데이터 수집 정상성 검증 보고서

- **분석 일자**: 2026-08-23
- **분석 대상**: `C:\ABCchallenges` (branch: `fix/kakao-geocoding-coords`, HEAD: `84e2363`)
- **분석 범위**: 전체 코드 정적 분석 + 저장된 데이터 파일 실측 + 외부 API 실호출 검증 + 수정 적용 (PART 5~8)
- **최종 갱신**: 2026-08-23 2차 세션 (카카오 로컬 전환·좌표·혼잡도 적용, PART 8 신규 / PART 9 = 잔여 과제)

> **표기 규칙**
> - `[확인]` = 코드 또는 데이터 파일을 직접 열어 검증한 사실
> - `[추정]` = 코드만 보고 짐작한 내용 (실행 검증 필요)

---

## 목차

- [PART 1. 전체 구조](#part-1-전체-구조)
- [PART 2. 수집 정상 작동 여부 (핵심)](#part-2-수집-정상-작동-여부-핵심)
- [PART 3. 실행 가능성 / 안정성](#part-3-실행-가능성--안정성)
- [PART 4. 종합 결론](#part-4-종합-결론)
- [PART 5. 미작동 API 수정 내역](#part-5-미작동-api-수정-내역-2026-08-23-적용)
- [PART 6. 좌표·태깅·점수 수정 내역](#part-6-좌표태깅점수-수정-내역-2026-08-23-적용)
- [PART 7. 허위 검증 문구·편의시설 수정 내역](#part-7-허위-검증-문구편의시설-수정-내역-2026-08-23-적용)
- [PART 8. 카카오 로컬 전환 및 파이프라인 재구성](#part-8-카카오-로컬-전환-및-파이프라인-재구성-2026-08-23-2차-세션-적용)
- [PART 9. 앞으로 수정해야 할 부분](#part-9-앞으로-수정해야-할-부분-2026-08-23-2차-세션-기준-최신)

---

# PART 1. 전체 구조

## 1) 디렉터리 구조 `[확인]`

```
C:\ABCchallenges\
├─ run_web.py                    # 로컬 웹서버 + 배치 실행 API (진입점)
├─ build.sh, vercel.json         # Vercel 배포 설정
├─ .github/workflows/deploy.yml  # GitHub Pages 자동 배포
├─ README.md                     # 2줄짜리 placeholder
├─ data_collection_report.md     # 기획/보고 문서
├─ realtime_data_plan.md         # 기획/보고 문서
│
├─ data/                         # ★ 실제 데이터 저장소 (전부 CSV/JSON 파일)
│   ├─ total_family_data.csv         (2497건)  ← 웹앱 메인 데이터
│   ├─ total_family_data.json        (2497건)  ← 위와 동일 내용
│   ├─ naver_search_results.csv      (1643건)
│   ├─ public_childcare_data.csv     ( 564건)
│   ├─ live_ticketlink_tickets.csv   ( 353건)
│   ├─ live_interpark_tickets.csv    ( 300건)
│   ├─ culture_events_raw.csv        ( 200건)
│   ├─ events.csv                    ( 906건)  ← 레거시/더미
│   ├─ real_time_metrics.csv         (  76건)  ← 레거시/방치
│   └─ weather.csv                   ( 119건)
│
├─ scripts/                      # ★ 실제 동작하는 수집 스크립트
│   ├─ config.json                        # ⚠ API 키 평문 저장 (git 커밋됨)
│   ├─ culture_event_collector.py   (333줄)
│   ├─ live_ticket_crawler.py       (163줄)
│   ├─ live_ticketlink_crawler.py   (182줄)
│   ├─ public_childcare_collector.py(174줄)
│   ├─ naver_search_collector.py    (168줄)
│   ├─ fetch_weather.py             (125줄)
│   ├─ generate_total_family_data.py(275줄)  # 1차 통합
│   ├─ enrich_total_family_data.py  (253줄)  # 2차 보강
│   └─ __pycache__/*.pyc                  # ⚠ 삭제된 .py의 컴파일 잔해가 git에 커밋됨
│
├─ web/                          # 현행 프론트엔드
│   ├─ index.html (244줄), admin.html (186줄)
│   ├─ app.js (1189줄), admin.js (419줄), style.css (946줄)
│   └─ allone_logo.png, logo_where.png
│
├─ web_ver0.1/                   # 구버전 프론트 사본 (어디서도 참조 안 됨)
│
├─ weekend_data_pipeline/        # ★ 사용되지 않는 별도 파이프라인 (~1,500줄)
│   ├─ collectors/  base.py, public_api.py, city_data.py,
│   │               performance_api.py, popup_crawler.py
│   ├─ database/    models.py (SQLAlchemy), session.py
│   ├─ config/settings.py, utils/helpers.py
│   └─ requirements.txt
│
└─ images_ref/                   # 카카오톡 캡처 이미지 6장
```

### 각 폴더의 역할

| 폴더 | 역할 | 실사용 여부 |
|---|---|---|
| `data/` | 모든 수집 결과의 저장소 (DB 대신 CSV/JSON 파일) | ✅ 사용 중 |
| `scripts/` | 실제 동작하는 수집·통합·보강 스크립트 8개 | ✅ 사용 중 |
| `web/` | 사용자/관리자 프론트엔드 | ✅ 사용 중 |
| `web_ver0.1/` | 구버전 프론트 사본 | ❌ 미참조 |
| `weekend_data_pipeline/` | SQLAlchemy 기반 별도 수집 아키텍처 | ❌ 데드코드 |
| `images_ref/` | 참고용 스크린샷 | — |
| `.github/workflows/` | GitHub Pages 배포 자동화 | ✅ 사용 중 |

### 사용 언어/프레임워크 `[확인]`

| 항목 | 내용 | 근거 |
|---|---|---|
| 백엔드 | Python 3.14.3, 표준 `http.server` (웹 프레임워크 없음) | `run_web.py:1-2` |
| 프론트 | 순수 HTML/CSS/Vanilla JS (빌드툴 없음) | `package.json` 부재 |
| 수집 | `requests` + `BeautifulSoup4` | 각 스크립트 import |
| 데이터 처리 | `pandas` | `generate_*.py`, `enrich_*.py` |
| 미사용 선언 | sqlalchemy, playwright, apscheduler, httpx, aiohttp, psycopg2 등 | `weekend_data_pipeline/requirements.txt` |

> **주의**: `requirements.txt`는 `weekend_data_pipeline/`에만 존재합니다.
> **실제로 돌아가는 `scripts/`용 의존성 파일은 없습니다.**

---

## 2) 실행 진입점 `[확인]`

### 실행 명령어

```bash
python run_web.py
```

실행 시 출력되는 주소:

```
로컬 웹 서버 주소:   http://localhost:8080/web/index.html
관리자 페이지 주소:  http://localhost:8080/web/admin.html   (1초 후 자동 브라우저 오픈)
배치 상태 API:      http://localhost:8080/api/batch_status
```

개별 수집기 단독 실행도 가능:

```bash
python scripts/live_ticket_crawler.py
python scripts/fetch_weather.py
# ... 등
```

### 프론트/백엔드 구성

**하나로 합쳐져 있습니다.** `run_web.py` 단일 프로세스가 정적파일 서버와 API 서버를 겸합니다.

| 엔드포인트 | 메서드 | 기능 | 정의 위치 |
|---|---|---|---|
| `/api/batch_status`, `/api/status` | GET | 배치 진행 상태 조회 | `run_web.py:148` |
| `/api/refresh` | POST | 7단계 수집 배치 백그라운드 실행 | `run_web.py:160` |
| `/api/weather` | POST | 날씨 갱신 (동기 실행) | `run_web.py:182` |
| 그 외 | GET | 정적 파일 서빙 | `run_web.py:156` |

### 배포 설정 — 경로가 2개 병존 `[확인]`

| 방식 | 파일 | 동작 |
|---|---|---|
| GitHub Pages | `.github/workflows/deploy.yml` | `main` 푸시 시 `web/` + `data/*.csv,json` → `_site/`, `sed`로 JS의 `../data/` → `./data/` 치환 |
| Vercel | `vercel.json` + `build.sh` | 동일 로직으로 `dist/` 생성. `/data/(.*)` 에 CORS + no-cache 헤더 부여 |

> ### ⚠ 치명적 구조 문제
>
> **배포본은 100% 정적입니다.** 두 배포 스크립트 모두 `run_web.py`를 복사하지 않습니다. `[확인]`
>
> 따라서 **운영 사이트에서 `/api/refresh`(수집 실행 버튼)와 `/api/weather`는 404**입니다.
> `admin.html`의 배치 실행 기능은 **로컬에서만 동작**합니다.

---

## 3) 데이터 계층 `[확인]`

### 실제 저장 방식

**DB가 아니라 `data/` 폴더의 CSV/JSON 파일입니다.**
프론트엔드가 CSV를 직접 `fetch`해서 브라우저에서 파싱합니다.

```javascript
// web/app.js:239
const familyRes = await fetch('../data/total_family_data.csv?t=' + Date.now());
```

### 정의만 되어 있고 사용되지 않는 스키마

`weekend_data_pipeline/database/models.py`에 SQLAlchemy 5개 테이블이 정의되어 있으나,
**어디서도 사용되지 않고 실제 DB 파일(`weekend_data.db`)도 존재하지 않습니다.**

| 테이블 | 주요 컬럼 |
|---|---|
| `places` | id, source, source_id, name, category, sub_category, address, road_address, latitude, longitude, region, district, phone, website, opening_hours, tags(JSON), target_audience(JSON), description, images, rating, review_count, has_parking, has_nursing_room, has_stroller_access, is_no_kids_zone, has_diaper_changing, has_kids_menu, congestion_level, congestion_rate, created_at, updated_at, is_active |
| `events` | id, source, source_id, title, place_id, place_name, address, latitude, longitude, start_date, end_date, category, sub_category, tags, target_audience, description, price, booking_url, images, created_at, updated_at, is_active |
| `performances` | id, source, performance_id, title, venue, venue_address, start_date, end_date, genre, state, cast, crew, runtime, age_limit, price_info, poster_url, detail_url, created_at, updated_at |
| `realtime_data` | id, place_id, place_name, area_name, congestion_level, congestion_rate, congestion_message, population, population_male, population_female, weather, temperature, collected_at |
| `collection_logs` | id, collector_name, status, items_collected, items_inserted, items_updated, error_message, started_at, finished_at, created_at |

### 실제 데이터 스키마 (현재 사용 중)

**`data/total_family_data.csv` — 15개 컬럼, 2,497행** `[확인]`

```
source_site, category, place_or_event_name, period, target_age, region,
fee_info, description, booking_url, ai_tags, crawled_at, theme_tags,
congestion_score, popularity_score, recommend_reason
```

카테고리 분포:

| 카테고리 | 건수 | 비율 |
|---|---:|---:|
| 자연친화 | 1,051 | 42.1% |
| 공공키즈카페 | 577 | 23.1% |
| 사설키즈카페 | 450 | 18.0% |
| 가족체험 | 217 | 8.7% |
| 문화생활 | 202 | 8.1% |
| **합계** | **2,497** | 100% |

`booking_url` 도메인 분포:

| 도메인 | 건수 |
|---|---:|
| `search.naver.com` | 1,632 |
| `icare.seoul.go.kr` | 291 |
| `gyeonggi.childcare.go.kr` | 263 |
| `culture.seoul.go.kr` | 188 |
| `www.ticketlink.co.kr` | 68 |
| `tickets.interpark.com` | 55 |

위경도 좌표 보유율: **1,355 / 2,497 (54.3%)**

---

# PART 2. 수집 정상 작동 여부 (핵심)

## 4) 수집 코드 전수 조사 `[확인]`

| # | 스크립트 | 대상 소스 | 호출 방식 | 저장 위치 | 1회 실행 시 호출 수 |
|---|---|---|---|---|---:|
| 1 | `culture_event_collector.py` | ① data.go.kr 공연전시 API<br>② 서울시 문화행사 API<br>③ MMCA(국립현대미술관)<br>④ SeMA(서울시립미술관)<br>⑤ 예술의전당<br>⑥ 네이버 데이터랩 | `requests` + `BeautifulSoup` | `culture_events_raw.csv` | 6 |
| 2 | `live_ticket_crawler.py` | `tickets.interpark.com/api/ranking` (6개 카테고리) | `requests` (JSON API) | `live_interpark_tickets.csv` | 6 |
| 3 | `live_ticketlink_crawler.py` | `ticketlink.co.kr/search/getSearchList` | `requests` (JSON API) | `live_ticketlink_tickets.csv` | **84** |
| 4 | `public_childcare_collector.py` | `search.naver.com` HTML 스크래핑 | `requests` + `BeautifulSoup` | `public_childcare_data.csv` | **165** |
| 5 | `naver_search_collector.py` | `search.naver.com` HTML 스크래핑 | `requests` + `BeautifulSoup` | `naver_search_results.csv` | **567** |
| 6 | `fetch_weather.py` | `api.open-meteo.com`<br>`air-quality-api.open-meteo.com` | `requests` | `weather.csv` | **34** |
| 7 | `generate_total_family_data.py` | (외부 호출 없음 — 1~5 병합) | `pandas` | `total_family_data.csv/.json` | 0 |
| 8 | `enrich_total_family_data.py` | `search.naver.com` (행별 1회) | `requests` + `BeautifulSoup` | `total_family_data.csv/.json` | **≈2,500** ⚠ |

**1회 전체 배치 실행 시 총 외부 요청: 약 3,360회** (그중 네이버 약 3,230회)

### 호출 규모 상세

- **#3 티켓링크 84회**: `0~9` + `a~z` + `ㄱ~ㅎ` + `가~하` + 키워드 15개 + 지역 17개 sweep (`live_ticketlink_crawler.py:33-40`)
- **#4 공공육아 165회**: 서울 25구 × 3키워드 + 경기 30시군 × 3키워드
- **#5 네이버 장소 567회**: 수도권 63개 시군구 × 9개 테마
- **#8 enrich 약 2,500회**: `total_family_data.csv` 전 행에 대해 1건씩 네이버 검색

### 실행 주기 — **스케줄러는 존재하지 않습니다** `[확인]`

- `apscheduler`가 `requirements.txt`에 선언되어 있으나 **어디에도 import·사용되지 않고, 패키지도 미설치**
- cron / systemd / Task Scheduler / GitHub Actions `schedule:` 트리거 **전부 없음**
- `deploy.yml`은 `push`와 `workflow_dispatch` 트리거만 있고 **데이터 수집을 하지 않음**
- `scripts/config.json`의 다음 설정값은 **읽어가는 코드가 없는 죽은 설정**입니다:
  ```json
  "congestion_update_interval_min": 30,
  "event_update_interval_hr": 6,
  "weather_update_interval_hr": 1
  ```

> **결론: 수집은 사람이 admin 페이지에서 버튼을 누르거나 스크립트를 직접 실행해야만 동작합니다.**
> 코드만 있고 자동 실행은 되지 않는 상태입니다.

---

## 5) 진짜 수집 vs 더미 데이터 — 코드 직접 확인 결과

### 5-A. 외부 호출 자체는 전부 "진짜"입니다 `[확인]`

전수 grep 결과:

- **HTTP 응답을 못 받았을 때 가짜 데이터로 채워넣는 폴백은 한 곳도 없습니다.** 실패 시 전부 빈 리스트(`return []`)를 반환합니다.
- `Faker`, `mock`, `sample_data`, `fake_data` 라이브러리 **사용 없음**
- 각 스크립트의 URL은 모두 실제 서비스 엔드포인트입니다.

### 5-B. 그러나 일부 필드값은 **랜덤 생성**입니다 `[확인]`

| 파일:라인 | 필드 | 코드 | 성격 |
|---|---|---|---|
| `generate_total_family_data.py:120` | `congestion_score` | `random.randint(1, 3)` | **폴백 아님. 항상 랜덤** |
| `generate_total_family_data.py:121` | `popularity_score` | `random.randint(75, 95)` | **항상 랜덤** |
| `generate_total_family_data.py:153` | `congestion_score` | `random.randint(2, 4)` | **항상 랜덤** |
| `generate_total_family_data.py:154` | `popularity_score` | `random.randint(60, 88)` | **항상 랜덤** |
| `generate_total_family_data.py:192` | `congestion_score` | `random.randint(3, 5)` | **항상 랜덤** |
| `generate_total_family_data.py:178` | `popularity_score` | `except: random.randint(65, 92)` | 폴백 (rank 파싱 실패 시) |
| `enrich_total_family_data.py:197,199` | `popularity_score` | `random.randint(70, 96)` | 폴백 (값 비정상 시) |
| `enrich_total_family_data.py:203,205` | `congestion_score` | `random.randint(1, 3)` | 폴백 (값 비정상 시) |

> **화면에 표시되는 "혼잡도"와 "인기도"는 실측값이 아니라 난수입니다.**
> 유일한 예외는 인터파크 데이터로, `rank`에서 `100 - rank*2`로 계산합니다 (`generate_total_family_data.py:177`).

**실제 데이터로도 확인됩니다:**

- `naver_search_collector.py:141-142` — 1,643건 전부 `congestion_score=2`, `popularity_score=85` **하드코딩**
- `public_childcare_collector.py:112-113` — 서울분 `congestion_score=2`, `popularity_score=92` **하드코딩**
- `public_childcare_collector.py:147-148` — 경기분 `congestion_score=2`, `popularity_score=90` **하드코딩**

그 결과 `total_family_data.csv` 2,497건 중 **1,671건(66.9%)의 인기도가 정확히 85**입니다.

`congestion_score` 실측 분포:

| 값 | 건수 |
|---:|---:|
| 2 | 1,870 |
| 3 | 291 |
| 1 | 189 |
| 4 | 101 |
| 5 | 46 |

### 5-C. `description` / `recommend_reason`은 100% 템플릿입니다 `[확인]`

`enrich_total_family_data.py:144-150`의 `generate_llm_description()`은 **이름과 달리 LLM을 호출하지 않는 f-string 템플릿**입니다.

```python
def generate_llm_description(name, cat, region, target_age):
    reg_clean = region.split('|')[0].strip() if region else "수도권"
    return (
        f"[{name}]는 {reg_clean} 지역에 위치한 주말 아이 동반 최적 명소입니다. "
        f"실시간 네이버 검색 기반으로 주차 및 수유실, 편의시설이 검증되었으며, "
        f"{target_age} 연령대 아이와 아빠가 함께 방문하기에 적극 추천합니다."
    )
```

**실측 결과:**

```
description 이 동일 템플릿 문구 사용:  2,497 / 2,497 건  (100%)
recommend_reason 고유값:               6 개  (2,497건 전체)
```

| 건수 | `recommend_reason` 값 |
|---:|---|
| 1,399 | 🅿️ 실시간 네이버 검증 주차 가능 & 🍼 수유실·기저귀갈이대 완비로 아기와 방문 최적 |
| 579 | 🧸 영유아 안심 실내 놀이기구와 부모 쉼터가 잘 갖춰진 키즈 파라다이스 |
| 194 | 🎨 아이들 눈높이에 맞춘 체험형 관람 코스로 교육과 재미를 동시 만족 |
| 160 | 👨‍👧 아빠와 아이가 주말에 부담 없이 특별한 추억을 만들 수 있는 베스트 추천지 |
| 151 | 🌊 시원한 물놀이와 야외 활동을 한 번에 즐길 수 있는 여름 인기 장소 |
| 14 | 🅿️ 네이버 지도 실검증 주차 지원 & 🚼 기저귀갈이대 완비로 편안한 나들이 |

### 5-C-1. ⚠ 특히 심각: "실시간 검증" 주장의 근거 부실

모든 설명문이 `"실시간 네이버 검색 기반으로 주차 및 수유실, 편의시설이 검증되었으며"`라고 주장합니다.
그러나 실제 검증 로직은 `enrich_total_family_data.py:84-87`입니다:

```python
has_parking  = any(k in html for k in ["주차", "주차장", "발렛", "무료주차", "주차가능", "주차지원"])
has_nursing  = any(k in html for k in ["수유실", "수유", "아기수유", "수유실완비"])
has_diaper   = any(k in html for k in ["기저귀갈이대", "기저귀 교환대", "기저귀대", "기저귀"])
has_stroller = any(k in html for k in ["유모차대여", "유모차 대여", "유모차 반입", "유모차"])
```

**네이버 검색 결과 페이지 HTML 전체 문자열에 `"주차"`라는 두 글자가 포함되는지만 검사합니다.**
검색 결과 페이지에는 광고·연관검색어·다른 장소 정보·블로그 스니펫이 섞여 있으므로
**해당 장소의 실제 주차 여부를 판별할 수 없습니다.**

실제로 2,497건 중 1,399건(56%)이 "주차 가능 & 수유실 완비"로 판정되었으나, **이 수치의 신뢰도는 사실상 없습니다.** `[확인 — 판정 로직이 부정확하다는 것은 확인 / 추정 — 실제 오탐률은 미측정]`

### 5-C-2. `enrich`가 공연/전시 원본 설명까지 덮어씀 `[확인]`

`enrich_total_family_data.py:208`은 **카테고리 분기 없이 전 행에 동일 템플릿을 적용**합니다.
따라서 티켓 상품 202건(문화생활)의 설명도 다음과 같이 왜곡됩니다:

```
"[2026 카즈미 타테이시 트리오 내한공연]는 강동아트센터 지역에 위치한
 주말 아이 동반 최적 명소입니다. ..."
```

### 5-D. `events.csv` / `real_time_metrics.csv`는 수작업 더미입니다 `[확인]`

`events.csv` 906건의 `source_url` 도메인 분포:

| 도메인 | 건수 |
|---|---:|
| `www.instagram.com` | 401 |
| `tickets.interpark.com` | 300 |
| `culture.seoul.go.kr` | 100 |
| `data.seoul.go.kr` | 52 |
| **`example.com`** | **27** |

- `https://example.com/events/1` 형태의 **플레이스홀더 URL 27건** 포함
- **이 두 파일을 생성하는 스크립트가 현재 코드베이스에 존재하지 않습니다.**
- git에는 `scripts/__pycache__/generate_mock_data.cpython-314.pyc`가 남아 있으나 원본 `.py`는 삭제된 상태
  → 과거 mock 생성기로 만들어진 잔재로 보임 `[추정]`

### 5-E. API 키 유효성 `[확인 — 형식만]`

`scripts/config.json`:

| 키 | 형식 | 실제 사용처 |
|---|---|---|
| `tour_api_key` (64자 hex) | 실제 발급값 형태 | `culture_event_collector.py:35` ✅ |
| `seoul_api_key` (30자) | 실제 발급값 형태 | `culture_event_collector.py:36` ✅ |
| `naver_client_id` / `naver_client_secret` | 실제 발급값 형태 | `culture_event_collector.py:37-38` ✅ |
| `kakao_api_key` (32자 hex) | 실제 발급값 형태 | ❌ **코드 어디서도 사용 안 함** |
| `tmap_api_key` | **`"YOUR_TMAP_API_KEY"` 플레이스홀더** | ❌ 사용처 없음 |
| `weather_api_key` | `tour_api_key`와 값이 동일 | ❌ `fetch_weather.py`는 키가 필요 없는 open-meteo 사용 |
| `ncp_naver_client_id` / `secret` (backup) | 실제 발급값 형태 | ❌ 사용처 없음 |

> **실제 값의 활성화 여부(유효성)는 호출해봐야 확정할 수 있어 미확인입니다.** → PART 2-6 승인 대기

### 5-F. 데이터로 역추적한 각 소스의 실제 성공 여부 `[확인]`

`culture_events_raw.csv` 200건의 `source` 컬럼 집계:

| 소스 | 수집 건수 |
|---|---:|
| 서울시 문화행사 | **200** (100%) |
| 공공데이터포털 (data.go.kr) | **0** |
| MMCA (국립현대미술관) | **0** |
| SeMA (서울시립미술관) | **0** |
| 예술의전당 | **0** |

> **6개 소스 중 서울시 API 1개만 성공했고, data.go.kr API와 미술관 크롤러 3개는 전부 0건입니다.**
> 크롤러들의 CSS 선택자(`.exhibition-list li`, `.list-gallery li`, `.performance-list li` 등)가
> 현재 사이트 구조와 맞지 않을 가능성이 높습니다. `[추정 — 0건인 것은 확인, 원인은 실행 검증 필요]`

또한 **네이버 데이터랩 트렌드 함수(`fetch_naver_trend`)는 정의만 되어 있고 `__main__` 블록에서 호출조차 되지 않습니다.** `[확인 — culture_event_collector.py:294-333]`

---

## 6) 실제 실행 검증 — **수행 완료** `[확인]`

- **수행 일시**: 2026-08-23
- **실제 호출**: **12회** (계획 10회 + 진단용 추가 2회 — 티켓링크 재시도, MMCA 선택자 확인)
- **데이터 파일 변경**: 없음 (콘솔 출력만)
- **비용**: 없음

### 검증 결과 총괄

| # | 대상 | HTTP | 결과 | 판정 |
|---|---|---:|---|---|
| A | data.go.kr 공연전시 API | **400** | `NO_OPENAPI_SERVICE_ERROR` / "해당 오픈API 서비스가 없거나 폐기됨" (code 12) | ❌ **엔드포인트 폐기** |
| B | 서울시 문화행사 API | 200 | `INFO-000 정상 처리`, `list_total_count = 19,503` | ✅ **정상 · 키 유효** |
| C | 네이버 데이터랩 API | 200 | 주간 트렌드 5개 구간 정상 반환 | ✅ **정상 · 키 유효** |
| D | 인터파크 랭킹 API | 200 | `concert` 키 5건, rank/goodsName/placeName 정상 | ✅ **정상** |
| E | 티켓링크 검색 API | (1차) 타임아웃 → (2차) 200 | 재시도 시 **0.11초**에 10건 반환 | ⚠️ **정상이나 일시 실패 발생** |
| F-1 | MMCA 국립현대미술관 | 200 | 페이지는 살아있음. 현행 선택자 0개 | ❌ **선택자 불일치 + JS 렌더링** |
| F-2 | SeMA 서울시립미술관 | **500** | 서버 오류 (HTML 1,704byte 에러 페이지) | ❌ **URL 무효/서버 오류** |
| F-3 | 예술의전당 | **404** | 페이지 없음 (HTML 1,968byte) | ❌ **URL 변경/삭제** |
| G | open-meteo 날씨 | 200 | 7일 예보 정상 (2026-08-23~08-29) | ✅ **정상** |
| H | 네이버 검색 스크래핑 | 200 | 장소명 46건 매칭. **단 좌표 추출이 26.7km 오차** | ⚠️ **부분작동 · 좌표 오류 확인** |

---

### [A] data.go.kr 공연전시 API — ❌ 엔드포인트 폐기 `[확인]`

```json
{ "OpenAPI_ServiceResponse": { "cmmMsgHeader": {
    "errMsg": "NO_OPENAPI_SERVICE_ERROR",
    "returnAuthMsg": "해당 오픈API 서비스가 없거나 폐기됨",
    "returnReasonCode": "12" } } }
```

> **인증 실패가 아니라 서비스 자체가 없어진 것입니다.**
> `tour_api_key`를 재발급해도 이 엔드포인트는 되살아나지 않습니다.
> **다른 API(KOPIS 공연예술통합전산망 등)로 교체하거나 해당 소스를 제거해야 합니다.**

### [B] 서울시 문화행사 API — ✅ 정상, 그러나 **95% 미수집** `[확인]`

```
RESULT     : {'CODE': 'INFO-000', 'MESSAGE': '정상 처리되었습니다'}
list_total : 19503
```

> **키는 유효하고 API도 정상입니다.**
> 다만 **총 19,503건이 제공되는데 `culture_event_collector.py:308`은 `rows=200`만 요청**합니다.
> 가용 데이터의 **약 1%만 수집**하고 있습니다. (신규 발견 이슈)

### [C] 네이버 데이터랩 API — ✅ 키 유효, 그러나 **호출 자체를 안 함** `[확인]`

```json
{"startDate":"2026-07-20","endDate":"2026-08-23","timeUnit":"week","results":[
  {"title":"키즈카페","data":[{"period":"2026-07-20","ratio":54.23},
   {"period":"2026-07-27","ratio":100},{"period":"2026-08-03","ratio":89.92},
   {"period":"2026-08-10","ratio":72.92},{"period":"2026-08-17","ratio":45.56}]}]}
```

> **키가 유효하고 API도 정상 동작합니다.**
> `fetch_naver_trend()` 함수는 완성되어 있으나 `__main__`에서 호출되지 않을 뿐입니다.
> **즉 "인기도를 난수로 채우는 문제(12-2)"의 실제 해법이 이미 코드 안에 있고, 키도 살아있고, 단지 연결만 안 되어 있는 상태입니다.**
> 이 한 줄 연결로 `popularity_score`를 실측값으로 대체할 수 있습니다.

### [D] 인터파크 랭킹 API — ✅ 정상 `[확인]`

```
top-level keys: ['concert']  → 5건
  rank 1  마룬5 내한공연 | 킨텍스 제1전시장 4,5홀 | 20270127 ~ 20270127
  rank 2  NCT DREAM 10TH ANNIVERSARY PARTY | 인스파이어 아레나 | 20260822 ~ 20260823
```

### [E] 티켓링크 검색 API — ⚠️ 정상이나 일시 실패 관측 `[확인]`

| 시도 | timeout | 결과 |
|---|---|---|
| 1차 | 8초 | `ReadTimeout` |
| 2차 | 30초 | HTTP 200, **0.11초**, 10건 반환 |

> **엔드포인트는 정상입니다.** 1차 실패는 일시적 현상이었습니다.
>
> **그런데 이것이 12-7번 위험의 실증 사례입니다.**
> 실제 스크립트는 `timeout=6`(1차 시도보다도 짧음)을 쓰고, 실패 시
> `live_ticketlink_crawler.py:157`의 `except Exception: pass`가 **로그 한 줄 없이**
> 해당 쿼리 결과를 통째로 버립니다. 84회 sweep 중 몇 건이 이렇게 조용히 사라졌는지
> **알 방법이 없습니다.**

### [F] 미술관/공연장 크롤러 3종 — ❌ 원인이 각각 다름 `[확인]`

| 소스 | HTTP | HTML 크기 | 진단 |
|---|---:|---:|---|
| MMCA | 200 | 157,492 | 페이지 정상. 현행 선택자 `.exhibition-list li` / `.exh-list li` → **0개**. 실제 DOM 클래스는 `exhibitionList`(camelCase)이나 **`.exhibitionList li`도 0개** → 목록이 JS로 렌더링됨 `[확인]`. `requests`+`BS4`만으로는 수집 불가 |
| SeMA | **500** | 1,704 | 서버 오류 페이지. URL이 무효하거나 서버 측 문제 |
| 예술의전당 | **404** | 1,968 | 페이지 없음. URL이 변경·삭제됨 |

MMCA 페이지의 유사 클래스 후보 (진단 출력):
`['barList', 'exhibitionList', 'fgList', 'formList', 'gnbList', 'item', 'linkList', 'tabList']`

> **3개 모두 "선택자만 고치면 되는" 문제가 아닙니다.**
> - MMCA → **Playwright 등 JS 렌더링 도구 필요** (또는 내부 JSON API 탐색)
> - SeMA / SAC → **URL부터 다시 찾아야 함**

### [G] open-meteo 날씨 — ✅ 정상 `[확인]`

```
예보 일수: 7   (2026-08-23 ~ 2026-08-29)
최고: [31.4, 32.5, 30.5, 28.9, 27.9, 28.4, 26.2]
최저: [23.1, 24.2, 24.0, 24.8, 24.1, 23.1, 23.8]
```

### [H] 네이버 검색 스크래핑 — ⚠️ 장소명은 정상, **좌표는 오류 확인** `[확인]`

검색어: `서울 강남구 아기랑 키즈카페`

**장소명 추출은 작동합니다** (46건 매칭):
```
'캘리클럽 역삼점네이버페이키즈카페,실내놀이터'    → 정제 후 '캘리클럽 역삼점'
'챔피언더블랙벨트 메가박스 코엑스몰점네이버페이…'  → 정제 후 '챔피언더블랙벨트 메가박스 코엑스몰점'
'릴리펏 청담본점키즈카페,실내놀이터'              → 정제 후 '릴리펏 청담본점'
```

#### 🔴 그러나 좌표 추출이 완전히 틀렸습니다 (신규 발견)

```
검색어      : 서울 강남구 아기랑 키즈카페
강남구 중심 : 37.4979, 127.0276
추출된 좌표 : 37.293541, 126.869366     ← enrich가 실제로 저장하는 값
직선 거리   : 26.7 km
```

`enrich_total_family_data.py:78-80`은 페이지 HTML 전체에서 정규식으로 **첫 번째로 나오는 `"y"` / `"x"` 값**을 그냥 가져옵니다. 검색 결과 페이지에는 광고·지도위젯·연관장소 등 **9개의 좌표 후보**가 섞여 있고, 그중 첫 번째가 해당 장소와 무관한 값이었습니다.

> **`total_family_data.csv`에서 좌표를 보유한 1,355건(54.3%)의 위경도는 신뢰할 수 없습니다.**
> 이 좌표는 "카카오맵/네이버맵 길찾기 완전 연동"(스크립트 docstring)에 쓰이도록 설계되어 있으므로,
> **사용자를 엉뚱한 장소로 안내할 수 있습니다.**
>
> 이 항목은 기존 보고서에서 `[추정]`으로 표기했던 것이 **`[확인]`으로 확정**된 사례입니다.

#### 편의시설 판정 로직도 실증되었습니다

강남구 키즈카페 검색 1건에 대한 실측:

| 검사 키워드 | HTML 포함 여부 |
|---|---|
| `"주차"` | **True** |
| `"수유실"` | **True** |
| `"기저귀"` | **True** |
| `"유모차"` | False |

> 특정 시설의 실제 보유 여부와 무관하게, **검색 페이지 어딘가에 그 단어가 있기만 하면 True**가 됩니다.
> 2,497건 중 1,399건(56%)이 "🅿️ 주차 가능 & 🍼 수유실·기저귀갈이대 완비"로 판정된 이유입니다.

> ⚠ 참고: H(네이버 스크래핑)는 네이버 이용약관상 자동 수집이 제한될 수 있습니다.
> 검증은 1회만 수행했습니다. **프로덕션에서 567~2,500회 반복하는 현재 설계는 차단·법적 리스크가 있습니다.**

---

## 7) 데이터 신선도(Freshness) `[확인]` — 기준일 2026-08-23

| 파일 | 마지막 갱신 | 경과 | 판정 |
|---|---|---:|---|
| `total_family_data.csv/.json` | 2026-08-22 (2,497건 전부 동일) | 1일 | ✅ 최신 |
| `naver_search_results.csv` | 2026-08-22 20:34:25 | 1일 | ✅ 최신 |
| `public_childcare_data.csv` | 2026-08-22 18:28:42 | 1일 | ✅ 최신 |
| `live_ticketlink_tickets.csv` | 2026-08-22 18:11:25 | 1일 | ✅ 최신 |
| `live_interpark_tickets.csv` | 2026-08-22 18:08:03~07 | 1일 | ✅ 최신 |
| `weather.csv` | 2026-08-22 16:38:56 (예보 08-22~08-28) | 1일 | ⚠ 오늘 예보는 포함되나 어제 시점 값 |
| `culture_events_raw.csv` | **`crawled_at` 컬럼 자체가 없음** | — | ⚠ 추적 불가 |
| `real_time_metrics.csv` | **2026-07-26 17:08:40** | **28일** | ❌ 방치 |
| `events.csv` | **타임스탬프 컬럼 없음** | — | ❌ 더미, 방치 |

> **전체적으로 메인 데이터는 하루 전 수집분으로 신선합니다.**
> 단, 이는 **자동 스케줄이 아니라 누군가 수동으로 배치를 돌린 결과**입니다 (4)항 참조).
> 레거시 파일 2개는 방치 상태입니다.

---

# PART 3. 실행 가능성 / 안정성

## 8) 의존성 및 import 검증 `[확인]`

### 현재 환경 설치 상태 (Python 3.14.3)

| 패키지 | 상태 | | 패키지 | 상태 |
|---|---|---|---|---|
| `requests` | ✅ OK | | `tenacity` | ❌ MISSING |
| `bs4` | ✅ OK | | `playwright` | ❌ MISSING |
| `pandas` | ✅ OK | | `apscheduler` | ❌ MISSING |
| `sqlalchemy` | ✅ OK | | `httpx` | ❌ MISSING |
| `loguru` | ✅ OK | | `aiohttp` | ❌ MISSING |
| `dotenv` | ✅ OK | | `fake_useragent` | ❌ MISSING |
| `numpy` | ✅ OK | | `psycopg2` | ❌ MISSING |
| `lxml`, `openpyxl` | ✅ OK | | | |

### 결론

**✅ `scripts/` 8개는 전부 정상 실행 가능합니다** (`requests`, `bs4`, `pandas`만 필요).

**❌ `weekend_data_pipeline/`은 실행 불가입니다.** 세 가지 원인이 겹칩니다:

1. **존재하지 않는 모듈 import** `[확인]`
   ```python
   # weekend_data_pipeline/collectors/__init__.py:6
   from .poi_collector import POICollector
   ```
   → `poi_collector.py` 파일이 **존재하지 않습니다.** 무조건 `ModuleNotFoundError`

2. **미설치 패키지**
   - `base.py:10` → `tenacity` MISSING
   - `popup_crawler.py:12` → `playwright` MISSING
   - `helpers.py:30` → `fake_useragent` MISSING

3. **진입점 부재** — `__main__` 블록도, 스케줄러도, 이 패키지를 호출하는 코드도 없습니다.

> **`weekend_data_pipeline/` 전체(약 1,500줄)는 현재 완전한 데드코드입니다.**

### requirements.txt와 실제 코드의 불일치

| 구분 | 항목 |
|---|---|
| 선언되었으나 미사용 | `psycopg2-binary`(DB 미사용), `alembic`, `httpx`, `aiohttp`, `apscheduler`, `aiofiles`, `pydantic`, `numpy` |
| 코드가 쓰지만 미선언 | `openpyxl` — 단, `live_ticket_crawler.py:30`이 `EXCEL_PATH`를 정의만 하고 실제 엑셀 저장 코드가 없어 실질 문제는 없음 |
| **누락** | **`scripts/`용 requirements.txt 자체가 없음** |

---

## 9) 에러 핸들링 점검 `[확인]`

### ✅ 타임아웃 — 양호

**전 호출 지점 14곳 모두 타임아웃이 설정되어 있습니다** (5~10초).

| 파일 | 라인 | timeout |
|---|---|---|
| `culture_event_collector.py` | 63, 100 | 10초 |
| `culture_event_collector.py` | 133, 163, 193, 255 | 8초 |
| `enrich_total_family_data.py` | 73 | 5초 |
| `fetch_weather.py` | 60 / 75 | 10초 / 5초 |
| `live_ticket_crawler.py` | 66 | 8초 |
| `live_ticketlink_crawler.py` | 111 | 6초 |
| `naver_search_collector.py` | 120 | 6초 |
| `public_childcare_collector.py` | 95, 130 | 6초 |

### ❌ 예외 처리 — 심각한 문제

| 위치 | 코드 | 문제 |
|---|---|---|
| `naver_search_collector.py:144` | `except Exception: pass` | **완전 무음.** 567회 요청이 전부 실패해도 로그 한 줄 없이 "0건 수집 완료"로 종료 |
| `public_childcare_collector.py:119` | `except Exception: pass` | 동일 |
| `public_childcare_collector.py:154` | `except Exception: pass` | 동일 |
| `live_ticketlink_crawler.py:157` | `except Exception: pass` | 동일 |
| `enrich_total_family_data.py:89` | `except Exception as e: pass` | 동일 (변수만 받고 사용 안 함) |
| `fetch_weather.py:82` | 맨 `except:` | 미세먼지 조회 실패 시 무조건 `"보통"`으로 기록 |
| `culture_event_collector.py:85,119,154,184,217,265` | `except Exception as e: print(...)` | ✅ 에러 출력함 |
| `live_ticket_crawler.py:128` | `except Exception as e: print(...)` | ✅ 에러 출력함 |

### ⚠ 서비스 전체가 죽지는 않으나, 그게 더 위험합니다

`run_web.py:112-121`이 각 배치를 `try`로 감싸 실패해도 다음 단계로 진행합니다. 그런데:

```python
subprocess.run([sys.executable, script_path], check=True, cwd=BASE_DIR)
현재step["status"] = "SUCCESS"
```

**수집 스크립트가 0건을 반환해도 exit code는 0이므로 admin 화면에 "SUCCESS"로 표시됩니다.** `[확인]`

그리고 `generate_total_family_data.py:264`는 CSV를 `to_csv()`(덮어쓰기 모드)로 저장합니다.

> ### 🔴 데이터 전멸 시나리오
> 수집이 전부 실패 → 각 CSV가 헤더만 남은 빈 파일로 덮어써짐 → `generate` 단계에서
> **기존 2,497건이 0건으로 날아감** → 화면 백지 → **admin은 여전히 "SUCCESS" 표시**
>
> 백업·롤백·건수 검증 로직이 **전혀 없습니다.** `[확인]`

### 추가로 발견한 버그 5건 `[확인]`

#### ① `live_ticket_crawler.py:115` — `timedelta` 미import (잠복 `NameError`)

```python
# 파일 상단: from datetime import datetime   ← timedelta 없음
"end_date": end_d or (datetime.now() + timedelta(days=60)).strftime("%Y-%m-%d"),
```
`end_date`가 빈 응답이 오는 순간 `NameError`로 해당 카테고리 전체가 유실됩니다.
(현재 데이터는 `end_date`가 다 차 있어 잠복 상태)

#### ② `web/app.js:246, 250-251` — `metricsData` 미대입 + 존재하지 않는 파일 참조

```javascript
const [p, e, m] = await Promise.all([
  fetch('../data/places.csv?t=' + Date.now()).then(...),          // ← 이 파일 존재하지 않음
  fetch('../data/events.csv?t=' + Date.now()).then(...),
  fetch('../data/real_time_metrics.csv?t=' + Date.now()).then(...),
]);
placesData  = p ? parseCSV(p) : [];
eventsData  = e ? parseCSV(e) : [];
// ⚠ metricsData = m ... 이 줄이 없음
```

결과:
- `metricsData`가 항상 빈 배열 → **혼잡도가 전부 `'LOW'` 고정** (`app.js:290`)
- `data/places.csv`가 **존재하지 않음** → `placesData = []` → `mergedData = []`
- → **커플/싱글/맛집 탭이 영구 빈 화면** (`app.js:381, 402`)

#### ③ `culture_event_collector.py:48-52, 301-305` — `area` 파라미터 미사용

```python
def fetch_culture_events_public_api(rows=100, area="서울"):   # area를 받지만
    ...                                                        # 함수 내부에서 전혀 사용 안 함

all_events += fetch_culture_events_public_api(rows=200, area="서울")   # [1]
all_events += fetch_culture_events_public_api(rows=200, area="경기")   # [2] ← [1]과 완전 동일한 중복 요청
```

#### ④ `culture_event_collector.py:113` — 필드 매핑 오류

```python
"realm": item.get("MAIN_IMG", ""),   # realm(장르 분야)에 이미지 URL을 넣음
```
실제 `culture_events_raw.csv`의 `realm` 컬럼이 이미지 URL로 채워진 것을 확인했습니다.

#### ⑤ `live_ticket_crawler.py:39-46 vs 102` — 딕셔너리 키 불일치

```python
RANKING_CATEGORIES = { ..., "KIDS": "가족/어린이/아동" }     # 키는 "KIDS"
...
family_score = 0.9 if ranking_type in ["FAMILY"] else (...)   # 검사는 "FAMILY"
```
**가족/어린이 카테고리 50건이 family 가중치 0.9를 받지 못하고 0.5로 처리됩니다.**

---

## 10) 보안 점검 `[확인]`

### 🔴 치명적: API 키 평문 커밋

**`scripts/config.json`이 git에 커밋되어 있습니다** (`git ls-files`로 확인).
실제 발급된 것으로 보이는 키 **7종**이 평문으로 들어 있습니다.

> 아래는 값을 마스킹한 목록입니다. 실제 값은 기재하지 않습니다.

| 파일:라인 | 키 이름 | 마스킹 값 |
|---|---|---|
| `scripts/config.json:3` | `tour_api_key` | `78f6…17fe` (64자 hex) |
| `scripts/config.json:4` | `seoul_api_key` | `4e44…5854` (30자) |
| `scripts/config.json:6` | `kakao_api_key` | `dc10…3625` (32자 hex) |
| `scripts/config.json:7` | `naver_client_id` | `Ify0…zHU` |
| `scripts/config.json:8` | `naver_client_secret` | `mrpA…HpB` |
| `scripts/config.json:12` | `ncp_naver_client_id` | `hg6m…eqh` |
| `scripts/config.json:13` | `ncp_naver_client_secret` | `mXDi…Dskr` |

**추가 위험 요소:**

- **`.gitignore` 파일이 아예 존재하지 않습니다.** `[확인]`
- **이 저장소는 GitHub Pages로 공개 배포 중입니다.** 저장소가 public이라면 키는 이미 노출된 것으로 간주해야 합니다.
- ✅ 다행히 `build.sh` / `deploy.yml`은 `web/`과 `data/`만 복사하므로 **`config.json`이 웹으로 서빙되지는 않습니다.** 그러나 **git 히스토리에는 남아 있습니다.**

### 🟠 기타 보안 이슈

| 항목 | 내용 |
|---|---|
| `.pyc` 커밋 | `scripts/__pycache__/` 4개 파일이 git에 커밋됨 (`generate_mock_data`, `crawler`, `db_manager`, `api_collector` — 원본 `.py`는 모두 삭제됨). 디컴파일 시 과거 로직·상수·엔드포인트가 드러날 수 있음 |
| CORS 전면 개방 | `run_web.py:197` — 모든 API 응답에 `Access-Control-Allow-Origin: *` |
| CSRF | `POST /api/refresh`에 인증이 전혀 없음. 임의 웹사이트가 로컬 서버의 배치 실행을 트리거 가능 |
| `.env` 부재 | `.env` 파일 없음. `weekend_data_pipeline/config/settings.py`는 `os.getenv` 방식으로 **올바르게 작성**되어 있음 → **아이러니하게도 안전한 쪽이 데드코드** |

---

# PART 4. 종합 결론

## 11) 데이터 수집 현황 요약표

> ⚠️ 아래 표는 **수정 전(2026-08-23 오전) 시점**의 진단 결과입니다.
> 이후 PART 5의 수정을 적용해 4개 소스가 복구되었습니다. 최신 상태는 [PART 5](#part-5-미작동-api-수정-내역-2026-08-23-적용) 참조.

| 수집 대상 | 작동 여부 | 마지막 성공 시각 | 근거 |
|---|---|---|---|
| **서울시 문화행사 API** | ✅ **정상 (단 1%만 수집)** → **PART 5에서 3,000건으로 상향** | 2026-08-22 | **실호출 [B] `INFO-000` 정상, 가용 19,503건.** 스크립트는 `rows=200`만 요청 |
| **인터파크 랭킹 API** | ✅ **정상** | 2026-08-22 18:08 | 6개 카테고리 × 50건 = 300건. **실호출 [D] 재확인** |
| **티켓링크 검색 API** | ✅ **정상 (일시 실패 관측)** | 2026-08-22 18:11 | 353건. **실호출 [E] 1차 타임아웃 → 2차 0.11초 성공.** `timeout=6` + `except: pass` 조합 위험 |
| **open-meteo 날씨** | ✅ **정상** | 2026-08-22 16:38 | 17지역 × 7일 = 119건. **실호출 [G] 재확인** |
| **네이버 검색 스크래핑 (장소)** | ⚠️ **부분작동** | 2026-08-22 20:34 | 1,643건 수집, 실존 상호명 확인됨. 단 점수는 전부 하드코딩(2/85) · **실호출 검증 [H]에서 장소명 46건 정상 매칭 확인** |
| **네이버 검색 스크래핑 (공공육아)** | ⚠️ **부분작동** | 2026-08-22 18:28 | 564건. 상동 (2/92, 2/90 하드코딩) |
| **위경도 좌표 (`region` 컬럼)** | 🔴 **오류 확정** | 2026-08-22 | **실호출 [H] 결과 강남구 검색에 26.7km 떨어진 좌표 반환.** 좌표 보유 1,355건(54.3%) 신뢰 불가 |
| **data.go.kr 공연전시 API** | ❌ **복구 불가 · 비활성화** | 기록 없음 | **실호출 [A] HTTP 400 `NO_OPENAPI_SERVICE_ERROR` — 서비스 폐기.** 키 재발급으로 해결 불가 |
| **MMCA 크롤링** | ✅ **PART 5에서 복구 (17건)** | 2026-08-23 00:53 | **실호출 [F-1] HTTP 200이나 목록이 JS 렌더링.** `requests`+`BS4`로 수집 불가 |
| **SeMA 크롤링** | ✅ **PART 5에서 복구 (8건)** | 2026-08-23 00:53 | **실호출 [F-2] HTTP 500.** URL 무효/서버 오류 |
| **예술의전당 크롤링** | ✅ **PART 5에서 복구 (19건)** | 2026-08-23 00:53 | **실호출 [F-3] HTTP 404.** URL 변경·삭제 |
| **네이버 데이터랩 트렌드** | ✅ **PART 5에서 연결 (1,153건 부여)** | 2026-08-23 00:53 | **실호출 [C] 정상 응답 확인.** 함수·키 모두 살아있고 `__main__` 연결만 누락 |
| **혼잡도 (`congestion_score`)** | 🎲 **더미** | — | `random.randint()` 4곳. 실측 소스 없음 |
| **인기도 (`popularity_score`)** | 🎲 **대부분 더미** | — | 66.9%가 하드코딩 85, 나머지 랜덤. 인터파크 rank 기반만 실측 |
| **편의시설 태그 (주차/수유실)** | 🎲 **신뢰불가** | 2026-08-22 | HTML 전체 문자열에 "주차" 포함 여부만 검사 |
| **`description` / `recommend_reason`** | 🎲 **더미** | — | 2,497건 100% 템플릿, 추천이유 고유값 6개 |
| **`real_time_metrics.csv`** | ❌ **방치** | 2026-07-26 | 28일 경과. 생성 스크립트 부재 |
| **`events.csv`** | ❌ **더미** | 불명 | `example.com` URL 27건 포함. 생성 스크립트 부재 |
| **`weekend_data_pipeline/` 전체** | ❌ **실행불가** | — | `poi_collector` 부재로 ImportError + 의존성 4종 미설치 + 진입점 없음 |

### 핵심 요약

> 장소·공연 데이터의 **"뼈대"(이름 · 장소 · 기간 · 예매 URL)는 진짜 실시간 수집이 맞습니다.**
>
> 그러나 **사용자에게 보이는 차별화 정보(혼잡도 · 인기도 · 추천이유 · 설명 · 편의시설)는 전부 생성된 값**입니다.

---

## 12) 보완이 필요한 부분 (심각도별)

### 🔴 치명적

| # | 항목 | 근거 |
|---|---|---|
| 1 | **API 키 7종이 git에 평문 커밋 + `.gitignore` 부재.** 저장소가 public이면 즉시 재발급 필요 | PART 3-10 |
| 2 | **혼잡도·인기도가 난수.** "실시간 혼잡도"를 표방하는 서비스의 핵심 지표가 `random.randint()` | PART 2-5B |
| 3 | **모든 설명문이 "실시간 네이버 검색 기반으로 검증되었으며"라고 주장하지만 실제 검증 로직이 부정확.** 사용자에게 검증되지 않은 정보를 검증된 것처럼 제시 | PART 2-5C1 |
| 4 | **수집 실패 시 기존 데이터 전멸 위험.** 덮어쓰기 + 백업 없음 + `except: pass`로 실패 은폐 + 0건이어도 "SUCCESS" 표시 | PART 3-9 |
| 5 | **배포본에서 admin 수집 기능이 동작하지 않음.** `run_web.py`가 배포되지 않는데 `admin.html`은 `/api/refresh`를 호출 | PART 1-2 |

### 🟠 중요

| # | 항목 | 근거 |
|---|---|---|
| 6 | **6개 문화 소스 중 4개(data.go.kr, MMCA, SeMA, 예술의전당)가 0건**, 네이버 데이터랩은 호출조차 안 됨 | PART 2-5F |
| 7 | **`except Exception: pass` 4곳** — 실패 원인 추적이 원천적으로 불가능 | PART 3-9 |
| 8 | **웹앱 커플/싱글/맛집 탭 영구 빈 화면** — 존재하지 않는 `places.csv` 참조 + `metricsData` 미대입 | PART 3-9 ② |
| 9 | **`live_ticket_crawler.py:115` `timedelta` 미import** — 잠복 `NameError` | PART 3-9 ① |
| 10 | **스케줄러 부재** — "실시간"을 표방하나 수동 실행만 가능 | PART 2-4 |
| 11 | **`enrich`가 공연/전시 원본 설명까지 덮어씀** (티켓 202건 왜곡) | PART 2-5C2 |
| 12 | **enrich 단계 네이버 요청 약 2,500회** — 캐시 파일(`naver_enrich_cache.json`)이 존재하지 않아 매 실행마다 전량 재요청. 차단 위험 높음 | PART 2-4 |
| 13 | **`real_time_metrics.csv` 28일 방치, `events.csv` 더미** — 생성 스크립트 부재 | PART 2-7 |

### 🟡 경미

| # | 항목 |
|---|---|
| 14 | `weekend_data_pipeline/` 약 1,500줄 데드코드 (import 자체가 깨짐) |
| 15 | `web_ver0.1/` 구버전 사본 방치 |
| 16 | `scripts/__pycache__/*.pyc` 4개 커밋됨 (원본 `.py` 삭제 상태) |
| 17 | `scripts/`용 `requirements.txt` 부재 |
| 18 | `config.json`의 `tmap_api_key`는 플레이스홀더, `kakao_api_key`·`weather_api_key`·`ncp_*`는 사용처 없음 |
| 19 | `config.json`의 `*_interval_*` 설정값을 읽는 코드 없음 |
| 20 | `culture_event_collector.py`의 `area` 파라미터 미사용 → 서울/경기 중복 호출 |
| 21 | `live_ticket_crawler.py`의 `"KIDS"` vs `"FAMILY"` 키 불일치 |
| 22 | `culture_event_collector.py:113` `realm` 필드에 이미지 URL 매핑 |
| 23 | `README.md` 2줄 (설치·실행 방법 없음), 배포 방식 2개 병존(GitHub Pages + Vercel) |
| 24 | `run_web.py` `/api/refresh` 인증 없음 (CSRF) |
| 25 | `culture_events_raw.csv`에 `crawled_at` 컬럼 없음 → 신선도 추적 불가 |

---

## 13) 다음 단계 제안 (우선순위순)

| 순위 | 작업 | 대응 항목 |
|---|---|---|
| **P0** | 노출된 API 키 5~7종 폐기·재발급 → 환경변수(`.env`)로 이전, `.gitignore` 작성, git 히스토리에서 `config.json`·`.pyc` 제거 | 12-1 |
| **P0** | 수집 결과 검증 게이트 추가: 신규 건수가 기존 대비 일정 비율(예: 50%) 미만이면 CSV 덮어쓰기 중단 + 기존 파일 백업 | 12-4 |
| **P0** | 랜덤 점수 처리 방침 결정 — ⓐ 서울시 실시간인구 API 등 실측 소스 연동 / ⓑ 리뷰수 기반 산출 / ⓒ **최소한 UI에 "참고용 추정치" 명시.** 현 상태 방치는 부적절 | 12-2 |
| **P1** | `description`/`recommend_reason` 템플릿의 "실시간 검증되었으며" 문구 제거 또는 실제 검증 구현 | 12-3 |
| **P1** | `except Exception: pass` 4곳을 로깅으로 교체 + 수집 건수 0이면 exit code 1 반환 → `run_web.py`가 ERROR로 인식하도록 | 12-7 |
| **P1** | 배포 아키텍처 결정 — 정적 배포 유지 시 admin 수집 UI를 로컬 전용으로 분리, 또는 서버리스 함수 / GitHub Actions cron으로 수집 이전 | 12-5, 12-10 |
| **P2** | 실패한 4개 소스 진단 후 선택자 수정 또는 제거. data.go.kr은 키 유효성부터 확인 (PART 2-6 승인 시 즉시 판별 가능) | 12-6 |
| **P2** | `app.js` 데이터 로드 버그 수정 (`metricsData` 대입, `places.csv` 참조 제거 또는 복원) | 12-8 |
| **P2** | `live_ticket_crawler.py`에 `timedelta` import 추가, `KIDS`/`FAMILY` 키 통일 | 12-9, 21 |
| **P2** | enrich 캐시 파일을 저장소에 커밋하거나 TTL 캐시로 전환 → 네이버 요청 2,500회 → 증분만 | 12-12 |
| **P3** | `weekend_data_pipeline/` · `web_ver0.1/` · `.pyc` 삭제 또는 살릴 것인지 결정 (현재는 완전 데드) | 12-14~16 |
| **P3** | `scripts/requirements.txt` 작성 (`requests`, `beautifulsoup4`, `pandas`), `README.md`에 실행법 기재 | 12-17, 23 |
| **P3** | `culture_events_raw.csv`에 `crawled_at` 컬럼 추가 | 12-25 |

---

# PART 5. 미작동 API 수정 내역 (2026-08-23 적용)

- **수정 파일**: `scripts/culture_event_collector.py` (전면 개편, v2.0)
- **수정 결과**: `data/culture_events_raw.csv` **200건 / 1개 소스 → 3,018건 / 4개 소스**
- **다운스트림 호환성**: `generate_total_family_data.py`의 `from_culture_events()` 27/27 정상 변환 확인 `[확인]`

## 14) 소스별 수정 결과

| 소스 | 이전 | 이후 | 원인 | 수정 방법 |
|---|---:|---:|---|---|
| 서울시 문화행사 | 200 | **3,000** | `rows=200` 고정 (서버 보유 19,503건) | 1,000건 단위 페이징 (`max_rows=3000`) |
| MMCA | 0 | **17** | 목록이 JS 렌더링 → `<div id="listDiv">`가 빈 상태로 전달 | 내부 JSON API `/exhibitions/AjaxExhibitionList.do` 사용 |
| SeMA | 0 | **8** | `/kr/exhibition/exhToday` → HTTP 500 | 메인 페이지의 `img[alt]`에서 제목 추출 + 상세 페이지에서 전시기간 파싱 |
| 예술의전당 | 0 | **19** | `/show/showList` → HTTP 404 (실제는 `show_list`) | 메인 페이지 파싱. 카드 마크업 3종 모두 처리 |
| 네이버 데이터랩 | 미호출 | **1,153건 부여** | 함수·키 모두 정상인데 `__main__` 연결만 누락 | 파이프라인에 연결, 전량(1,533키워드/307요청) 조회 |
| data.go.kr | 0 | **0** | **서비스 폐기** (`NO_OPENAPI_SERVICE_ERROR`) | ❌ **복구 불가** — 아래 참조 |

### ❌ data.go.kr은 코드 수정으로 해결할 수 없습니다

`returnAuthMsg="해당 오픈API 서비스가 없거나 폐기됨"` (code 12).
인증 실패가 아니라 **서비스 자체가 사라진 것**이므로 `tour_api_key`를 재발급해도 복구되지 않습니다.
현재는 `PUBLIC_API_ENABLED = False`로 비활성화하고 폐기 사실을 코드 주석에 기록했습니다.
살리려면 **KOPIS 공연예술통합전산망 등 다른 서비스의 신규 키 발급이 필요**합니다.

## 15) 함께 수정한 결함

| 항목 | 이전 | 이후 |
|---|---|---|
| `realm` 컬럼 | `MAIN_IMG`(이미지 URL)를 매핑 — 값이 URL로 오염 | `GUNAME`(자치구명)으로 수정 |
| `crawled_at` 컬럼 | 없음 → 이 파일만 신선도 추적 불가 | 추가 |
| 타임아웃 | 6~8초, 재시도 없음 | `http_get()` 래퍼로 15~20초 + 재시도 2회 |
| 실패 로깅 | 일부 소스가 조용히 실패 | **재시도 후에도 실패하면 사유 필수 출력** |
| 0건일 때 동작 | 무조건 덮어써서 기존 데이터 유실 위험 | **덮어쓰기 중단 + `exit 1`** (배치가 SUCCESS로 오인하지 않도록) |
| 데이터랩 폭주 방지 | — | `TREND_MAX_REQUESTS=1200` 상한, 초과 시 **잘라낸 사실을 로그로 명시** |
| 429 대응 | — | 감지 즉시 중단 (남은 요청을 계속 던지지 않음) |

## 16) 네이버 데이터랩 쿼터 — 확인된 것과 아닌 것

**`[확인]` 직접 검증한 사실**

| 항목 | 결과 |
|---|---|
| 요청당 키워드 그룹 수 | **5개** (실제 5개 요청 → 5개 응답) |
| 응답 헤더의 쿼터 정보 | **없음** (`X-RateLimit-*` 류 부재) → 잔여 호출수를 프로그램으로 추적 불가 |
| 307회 연속 호출 | **429 미발생** → 일일 한도가 최소 307회보다 훨씬 위임을 실증 |
| 지수 확보율 | 키워드 1,533개 요청 → **362개만 응답**. 검색량이 낮으면 데이터랩이 값을 주지 않음 |
| 이벤트 부여율 | 3,018건 중 **1,153건 (38.2%)**. 이것이 사실상 천장 |

**일일 허용량 = 1,000회 `[확인 · API 응답으로 확정]`**

전량 수집 중 쿼터가 소진되며 API가 정확한 수치를 반환했다:

```json
HTTP 429
{"errorMessage":"{count/quota=1000/1000} : Query limit exceeded.
                 (쿼리 한도를 초과했습니다.)","errorCode":"010"}
```

> **네이버 데이터랩 검색어트렌드 API의 일일 허용량은 1,000회다.**
> 웹 검색에서 나오던 "25,000회"는 데이터랩에 해당하지 않는 수치였다.
> 초기 보고서의 "일 1,000회" 기재가 결과적으로 정확했으며, 이를 근거 부족으로
> 철회했던 중간 판단이 잘못이었다. 지금은 API 응답 자체가 근거다.

**운영상 함의**

| 항목 | 값 |
|---|---:|
| 일일 허용량 | 1,000회 |
| 전량 수집 1회 소요 (앵커 방식) | **384회** |
| 하루 최대 실행 가능 횟수 | **2회** (768회) |

> ⚠ `run_web.py`의 배치를 하루 3회 이상 돌리면 쿼터가 소진되어
> 트렌드 지표가 부분 수집으로 끝난다. 배치 주기 설계 시 반드시 고려해야 한다.

## 17) ⚠️ `popularity` 값 해석 시 주의 — 그룹 내 상대값

데이터랩은 **절대 검색량을 주지 않습니다.** 요청 1건에 포함된 5개 키워드 × 전체 주차 중
**최대 지점을 100으로 놓고 나머지를 비율 환산**합니다. 즉 값의 기준(尺)이 요청마다 다릅니다.

**실측 증명** `[확인]` — 동일 키워드 `예술의전당`, 동일 기간:

| 함께 묶인 키워드 | `예술의전당` 값 |
|---|---:|
| 세종문화회관, 국립극장, 블루스퀘어, LG아트센터 | **80.4** |
| 날씨, 유튜브, 쿠팡, 네이버 | **0.3** |

현재 구현은 키워드를 수집 순서대로 5개씩 자르므로 **어떤 키워드끼리 묶이는지가 임의적**입니다.
따라서 `popularity=100`은 "전체 1위"가 아니라 **"같은 요청에 묶인 5개 중 1위"**일 뿐입니다.

### ✅ 해결: 앵커(기준 키워드) 정규화 적용

모든 요청에 동일한 앵커 키워드 `공연`을 1개씩 넣고 **(키워드값 / 앵커값)** 비율을 쓴다.
데이터랩이 요청마다 다르게 잡는 정규화 계수가 분자·분모에서 상쇄되므로,
이 비율은 요청 조합과 무관한 **절대 비교값**이 된다.

**앵커 선정 근거** `[확인]` — 후보별 기간평균값:

| 후보 | 앵커값 | 판정 |
|---|---:|---|
| **공연** | **39.43** | ✅ 채택. 소형 키워드도 0.0146 반환 |
| 전시 | 15.42 | 가능 |
| 나들이 | 4.12 | 비율이 커짐 |
| 주말 | 3.25 | 비율이 커짐 |
| 문화행사 | 0.04 | ❌ 비율이 수천대로 튀어 정밀도 악화 |

**적용 후 검증** `[확인]` — 동일 키워드 `예술의전당`을 전혀 다른 조합에 배치:

| 함께 묶인 키워드 | 적용 전 원값 | **적용 후 비율** |
|---|---:|---:|
| 세종문화회관·국립극장·블루스퀘어 | 80.4 | **2.310087** |
| 날씨·유튜브·쿠팡 | 0.3 | **2.310139** |
| 차이 | **268배** | **0.002%** |

**출력 컬럼 2종**

| 컬럼 | 성격 | 용도 |
|---|---|---|
| `trend_ratio` | 앵커 대비 배수. 상한 없는 실수 | **절대 비교·회차 간 비교 가능.** 예) 2.31 = 앵커의 2.31배 |
| `popularity` | `trend_ratio`를 log10 min-max로 0~100 환산한 정수 | UI 표시용. **수집 회차 내부의 상대 순위**이므로 회차 간 비교 불가 |

로그 스케일을 쓴 이유: 검색량 분포가 로그에 가까워 선형 환산 시 상위 소수가
전 구간을 잡아먹어 변별력이 사라진다.

**측정값 없음 처리**: 데이터랩은 검색량이 임계 미만이면 값을 주지 않는다.
이를 0으로 저장하면 "측정된 최하위"와 "미측정"이 구분되지 않으므로 **공란**으로 남긴다.

---

# PART 6. 좌표·태깅·점수 수정 내역 (2026-08-23 적용)

PART 5는 `culture_event_collector.py` 한 파일이었고, 여기서는 나머지 수집기의
**조작된 데이터**를 제거했다. 수정 순서는 심각도 순(좌표 → 인터파크 → 점수)이다.

## 18) 다른 수집기 실호출 점검 결과 `[확인]`

**엔드포인트는 전부 정상이었다. 문제는 전부 로직 쪽이다.**

| 수집기 | 실호출 | 결과 |
|---|---|---|
| 인터파크 랭킹 API | 200 | 카테고리당 50건 |
| 티켓링크 검색 API | 200 | 3개 쿼리 모두 성공 |
| 네이버 장소 스크래핑 | 200 | 실존 상호명 10개 추출 |
| 공공육아 스크래핑 | 200 | 유효 시설 5개 |
| open-meteo 날씨 | 200 | 7일 예보 + pm10=17.6 |

## 19) 🔴 좌표 — 검색어와 무관한 단일 고정값이었음

### 실측 증거

5개 지역 검색이 **전부 동일 좌표**를 반환했다:

| 검색어 | 추출 좌표 | 오차 |
|---|---|---:|
| 서울 강남구 캘리클럽 역삼점 | (37.2935, 126.8694) | 26.7 km |
| 서울 노원구 키즈카페 | (37.2935, 126.8694) | 43.4 km |
| 인천 연수구 키즈카페 | (37.2935, 126.8694) | 21.3 km |
| 경기 수원시 키즈카페 | (37.2935, 126.8694) | 14.5 km |
| 서울 마포구 공원 | (37.2935, 126.8694) | 30.5 km |

저장 데이터도 동일했다: 좌표 보유 **1,355건 중 고유값 3개**,
그중 **1,353건(99.9%)이 `(37.485305, 126.866500)` 하나**.

> `enrich_total_family_data.py`가 네이버 검색 HTML에서 **첫 번째 `"y"`/`"x"`를
> 그냥 집어왔는데**, 그 값은 장소 좌표가 아니라 페이지 템플릿의 고정 요소였다.
> "장소별 좌표"가 아니라 **"그날 네이버 페이지의 상수"를 1,355번 복사**한 것이다.
> 스크립트 docstring의 "카카오맵/네이버맵 길찾기 완전 연동"은 성립하지 않는다.

### 수정

- 정규식 스크래핑 제거 → **카카오 로컬 API 정식 지오코딩**으로 교체
- **좌표를 못 얻으면 반드시 공란.** 추측값을 넣지 않는다
- 저장 데이터의 잘못된 좌표 **1,355행 제거** (CSV/JSON 양쪽)

### ⚠️ 남은 조치 — 사용자 작업 필요

```
HTTP 403 {"errorType":"NotAuthorizedError",
          "message":"App(아빠어디가) disabled OPEN_MAP_AND_LOCAL service."}
```

**카카오 키 자체는 유효하나 앱에서 지도/로컬 서비스가 꺼져 있다.**
`developers.kakao.com` → 내 애플리케이션 → **아빠어디가** → 카카오맵/로컬 활성화
가 필요하다. 켜면 코드 수정 없이 바로 좌표가 채워진다.
그전까지는 좌표가 공란으로 유지된다(가짜 값 생성 안 함).

## 20) 🟠 인터파크 가족 태깅 누락

```python
RANKING_CATEGORIES = { ..., "KIDS": "가족/어린이/아동" }   # 키는 KIDS
family_score = 0.9 if ranking_type in ["FAMILY"] else ...  # 검사는 FAMILY
```

**실측: KIDS 50건 중 `family:0.9` 부여 0건** — 전부 `family:0.5`였다.
가족·어린이 공연이 가족 추천에서 불리하게 취급되고 있었다.

| 항목 | 수정 전 | 수정 후 |
|---|---:|---:|
| KIDS 50건 중 family:0.9 | **0건** | **50건** |
| CONCERT 50건 family:0.5 | 50건 | 50건 (회귀 없음) |

`timedelta` 미import도 함께 수정했다(잠복 `NameError`).

## 21) 🔴 조작 점수 전량 제거

### 제거한 것

| 파일 | 기존 | 수정 후 |
|---|---|---|
| `naver_search_collector.py` | `congestion=2, popularity=85` 하드코딩 (1,643건 전부) | 공란 |
| `public_childcare_collector.py` | `2/92`, `2/90` 하드코딩 | 공란 |
| `generate_total_family_data.py` | `random.randint()` 4곳 | 제거, 원본 통과 |
| `enrich_total_family_data.py` | `random.randint()` 폴백 4곳 | 제거, 공란 유지 |

`scripts/` 전체에 **`random.` 사용 0건**으로 확인됐다.

**인터파크 `rank` 기반 인기도는 실측이므로 보존**했다(`100 - rank*2`).

### 저장 데이터 정리

| 파일 | 조치 |
|---|---|
| `naver_search_results.csv` | 3,286개 셀 비움 |
| `public_childcare_data.csv` | 1,128개 셀 비움 |
| `total_family_data.csv/.json` | congestion 전량 제거, popularity 2,442건 제거 · **인터파크 55건 보존** |

### 프론트엔드도 함께 수정 (필수)

파이썬에서 조작을 없애도 `app.js`가 대신 지어내고 있었다:

```javascript
const congestion = parseInt(item.congestion_score) || 1;   // 공란 → 1 → 'LOW'(여유)
```

**측정된 적 없는 장소가 "여유"로 표시**되던 문제다. 다음을 수정했다:

- `app.js`: 공란 → `UNKNOWN` (혼잡도 뱃지·상세시트 양쪽)
- `app.js`: 인기도 공란 → `정보 없음` 표기
- `CROWD_LABEL`에 `UNKNOWN: '정보 없음'` 추가
- `style.css`에 `.crowd-dot.UNKNOWN` / `.crowd-val.UNKNOWN` 회색 스타일 추가

## 22) 🟠 티켓링크 커버리지 3% (구조적 한계 · 미해결)

응답에 `totalResultCount: 1118` 인데 **쿼리당 10건만 반환**한다.
페이징 파라미터 4종(`page`, `pageNo`, `size`, `pageSize`)을 모두 시도했으나
**전부 동일 결과**였다.

> 84회 sweep을 돌려도 **최대 840건**(중복 제거 후 353건)이 상한이다.
> sweep 키워드를 늘리는 것 외에 방법이 없고, 현재 구조로는 전수 수집이 불가능하다.
> **미해결 상태로 남긴다.**

## 23) 수정 후 데이터 상태 `[확인]`

```
data/total_family_data.csv  (2,497행)
  congestion_score 값 있음 :     0건   (실측 소스 없음 → 전량 공란)
  popularity_score 값 있음 :    55건   (전부 인터파크 rank 기반 실측)
  region 좌표 잔존         :     0건   (1,355건 제거 완료)
```

"측정하지 않은 값을 실측처럼 보여주던" 상태는 해소됐다.
대신 **표시할 수 있는 지표가 크게 줄었다.** 이는 데이터 손실이 아니라
원래부터 없던 정보가 드러난 것이다. 실제 지표가 필요하면:

| 지표 | 가능한 실측 경로 |
|---|---|
| 인기도 | 네이버 데이터랩 (앵커 정규화 방식, PART 5-17 참조) — 일 1,000회 한도 |
| 혼잡도 | 서울시 실시간 도시데이터 API 등 별도 도입 필요 (현재 프로젝트에 소스 없음) |
| 좌표 | 카카오 로컬 API — **앱에서 서비스 활성화만 하면 즉시 동작** |

---

# PART 7. 허위 검증 문구·편의시설 수정 내역 (2026-08-23 적용)

PART 6이 좌표·태깅·점수였다면, 여기서는 **"실시간 네이버 검증"이라고 주장하던
설명문과 편의시설 판정**을 다뤘다. 셋 다 같은 계열의 문제(측정하지 않은 것을
측정한 것처럼 표시)이며, 이로써 해당 계열은 정리가 끝났다.

## 24) 🔴 편의시설 판정이 구조적으로 항상 참이었음 `[확인]`

### 근본 원인

기존 코드는 1.6MB 검색 HTML **전체**에 단어가 있는지만 봤다.

```python
has_nursing = any(k in html for k in ["수유실", "수유", ...])
```

실측 결과, `수유실`은 페이지에 **항상 존재하는 JS 라벨 사전**에 들어 있었다:

```json
{"rest_area":"쉼터","nursing_room":"수유실","hospital":"병원"}
```

> **즉 `has_nursing`은 장소와 무관하게 전 건 `True`였다.**
> `주차`도 124회 등장 중 대부분이 방문자 리뷰·광고 문구였다.
> 그 결과 2,497건 중 **1,399건(56%)이 "주차 가능 & 수유실·기저귀갈이대 완비"**로 표시됐다.

### 수정 — 구조화 필드 파싱으로 교체

네이버 플레이스에는 장소별 편의시설 배열이 따로 있다:

```json
"conveniences": ["단체 이용 가능","무선 인터넷","남/녀 화장실 구분","유아시설 (놀이방)","주차"]
```

- 이 배열에 **명시된 항목만** 인정한다
- 배열이 없으면 `True/False`가 아니라 **미확인**(`amenities_known=False`)
- 오탐이 구조적으로 불가능하다

### 검증 불가로 판정해 제거한 태그

| 태그 | 제거 사유 |
|---|---|
| `nursing_room:1.0` | 카테고리가 키즈카페라는 이유만으로 부여했다. 게다가 **네이버 conveniences 어휘에 수유실 항목 자체가 없어** 어떤 방법으로도 검증 불가 |
| `diaper_table:1.0` | 위와 동일 |
| `stroller:1.0` | 검증 불가 |
| `parking:0.8` | 미확인인데 `0.8`이라는 그럴듯한 수치를 붙였다 |

## 25) 🔴 설명문·추천이유의 허위 주장 제거

| 항목 | 수정 전 | 수정 후 |
|---|---|---|
| `description` | 전 건 "실시간 네이버 검색 기반으로 주차 및 수유실, 편의시설이 **검증되었으며**" | 해당 문구 삭제. **원본 설명이 있으면 보존** |
| `recommend_reason` | "🅿️ **실시간 네이버 검증** 주차 가능 & 🍼 수유실·기저귀갈이대 **완비**" | 확인된 것만 "🅿️ 주차 가능 (네이버 플레이스 등록 정보)". **미확인이면 편의시설을 아예 언급하지 않음** |

### 원본 설명 보존 시 발견한 함정

enrich는 `total_family_data.csv`를 **읽고 같은 파일에 쓴다.** 그래서 "원본 보존"
로직을 그냥 넣으면, 이미 저장돼 있던 **옛 템플릿 문구를 '원본'으로 착각해 영구 보존**하게 된다.
레거시 템플릿 지문(`_LEGACY_DESC_MARKERS`) 판별을 넣어 차단했다.

기존에는 공연·전시 **202건의 원본 설명까지 덮어써서** "OO 지역에 위치한 주말 아이
동반 최적 명소"로 왜곡되고 있었다. 이제 원본이 유지된다.

## 26) 저장 데이터 정리 `[확인]`

| 대상 | 처리 |
|---|---:|
| `description` 허위 템플릿 | **2,497행** 제거 (100%) |
| `recommend_reason` 허위 문구 | **1,413행** 제거 |
| `ai_tags` 근거 없는 태그 | **2,307행** 정리 |

잔존 검사: `실시간 네이버` 0건 · `nursing_room` 0건 · `diaper_table` 0건.

프론트는 `description` 공란을 "설명 정보가 없습니다."로 이미 처리하고 있어
추가 수정이 필요 없었다.

## 27) 🟠 편의시설 커버리지 8% — 병목은 소스가 아니라 장소명 `[확인]`

편의시설 데이터는 **분명히 존재한다.**

```
고양어린이박물관 → 예약, 무선 인터넷, 남/녀 화장실 구분, 유아시설(놀이방),
                  단체 이용 가능, 유아의자, 대기공간, 주차
```

`유아의자`, `대기공간`, `유아시설` 등 이 서비스에 딱 맞는 항목들이다.
그런데 25건 샘플 실측 커버리지는 **8%**에 그친다.

### 조회를 막던 버그 2개 (수정 완료)

**① 질의 중복** — `region` 컬럼이 이미 장소명을 포함한다
(`region="서울 강남구 캘리클럽 역삼점"`). 여기에 장소명을 또 붙여
`"서울 강남구 캘리클럽 역삼점 캘리클럽 역삼점"` 으로 검색하고 있었다.

| 질의 | conveniences |
|---|---|
| `서울 강남구 캘리클럽 역삼점 캘리클럽 역삼점` | **없음** |
| `캘리클럽 역삼점` | ✅ 5개 항목 |

**② 장소명 손상** — 접미사 중복 제거(`dedupe_repeated_suffix`) 추가

| 원본 | 정규화 후 |
|---|---|
| `고양어린이박물관박물관` | `고양어린이박물관` ✅ |
| `남양주시육아종합지원센터육아종합지원센터` | `남양주시육아종합지원센터` |
| `500만 명 찾은 서울상상나라 국내` | `서울상상나라` ✅ |

**③ 질의 전략** — 25건 실측 비교: 지역+장소명 4% vs **장소명 단독 8%**.
`region`의 지역 배정 자체가 틀린 경우가 있어(동작구 시설이 관악구로 기록)
접두어를 붙이면 오히려 플레이스 카드가 안 뜬다. 장소명 단독을 기본으로 설정했다.

### 남은 병목 — `naver_search_collector.py` 의 이름 추출

정규화로도 복원 불가능한 손상이 다수다:

```
군포장난감도서관산본점장난감대여      ← 어절이 통째로 엉킴
행복장난감도서관장난감
다산생태공원자연공원
오션키즈강동점수영장
안양에는 무료 박물관과 환경           ← 블로그 제목 조각
2일 전인천 중구신도시북로 31-35 유수지공원
```

> **편의시설 소스가 없는 게 아니라, 쓰레기 이름으로 조회하고 있는 것이다.**
> 앵커 텍스트를 이어 붙이면서 어절이 엉키고 블로그 제목이 장소명으로 들어온다.
> 근본 해결은 스크래퍼 이름 추출 재작성이지만, 사용자는 우선 카카오 로컬
> 활성화(비용이 낮고 좌표도 함께 해결)를 선택했다.

### 대체 소스 탐색 결과 `[확인]`

| 소스 | 결과 |
|---|---|
| 네이버 지역검색 API | ❌ `Scope Status Invalid` — 앱에 검색 API 스코프 미등록 |
| 네이버 지도 내부 API | ❌ HTTP 400 / 404 |
| data.go.kr 전국수유실표준데이터 | ❌ `NO_OPENAPI_SERVICE_ERROR` (경로 폐기) |
| data.go.kr 전국도시공원표준데이터 | ⚠️ `SERVICE_KEY_IS_NOT_REGISTERED` — **활용신청하면 사용 가능** |
| 서울 열린데이터광장 `ChildCareInfo` | ✅ 9,490건 (어린이집 · 놀이터수/CCTV/좌표 보유) — 단 우리 데이터와 대상이 다름 |
| 서울 열린데이터광장 `GetParkInfo` | ✅ 2,189건 (공영주차장) |
| 카카오 로컬 API | ⚠️ 키 유효하나 **서비스 비활성** — 활성화 시 사용 가능 |

---

# PART 8. 카카오 로컬 전환 및 파이프라인 재구성 (2026-08-23 2차 세션 적용)

PART 5~7 이후 같은 날 진행한 2차 작업이다.
커밋 `eb1cb2d` ~ `84e2363` (브랜치 `fix/kakao-geocoding-coords`).

## 28) 세션 전후 비교 `[확인]`

| 항목 | 2차 세션 전 | 현재 |
|---|---|---|
| 데이터 건수 | 2,497건 | **11,020건** |
| 좌표 보유 | **0%** (전량 공란) | **89.2%** (9,834건) |
| 손상 장소명 | 다수 (채용공고·URL·문장) | **0건** |
| 커플/싱글/맛집 탭 | 영구 빈 화면 | 복구 (76건) |
| 수집 실패 시 | 데이터 전멸 + SUCCESS 표시 | 6개 전부 보존 + exit 1 |
| `except Exception: pass` | 4곳 | **0곳** |
| 배치 파이프라인 | 7단계 | **11단계** |
| 혼잡도 실측 | 소스 없음 | 2,258건 (서울시 실시간) |
| 편의시설 | 8% (측정 무효) | 166건 검증 확보 (한계 확정) |

## 29) 카카오 로컬 — 콘솔 토글이 아니라 키 재발급으로 해결 `[확인]`

구 PART 8 에 "콘솔에서 `OPEN_MAP_AND_LOCAL` 서비스를 켜야 한다"고 적었으나,
실제로는 **사용자가 키를 새로 발급해 `config.json` 을 교체**해서 풀렸다.
기존 키는 계속 `403 NotAuthorizedError` 였다.

## 30) 좌표 확보 — 세 경로로 89.2% `[확인]`

| 경로 | 대상 | 결과 |
|---|---|---|
| 수집 단계에서 확보 | 장소·공공육아 수집기 | 8,006 + 498건 **100%** |
| 공연장(venue) 조회 | 공연·행사 | 2,130건 중 1,958건 (91.9%) |
| 남은 미보유 | 1,186건 | 전부 행사 — `각 기관`·`지하 문화강좌실`·`4층 어린이자료실` 처럼 공연장이 아닌 호실명이라 해결 수단 없음 |

**좌표 없는 '장소' 행은 0건이다.**

### 공연장은 이미 `region` 컬럼에 있었다

공연·행사는 `place_or_event_name` 이 공연 제목이라 지오코딩 대상이 없었는데,
공연장은 처음부터 `region` 에 들어 있었다 (`강동아트센터 대극장 한강`).
**조회 대상 필드가 틀렸을 뿐 venue 를 새로 수집할 필요가 없었다.**

## 31) 장소 수집기 재작성 (구 P2-1) `[확인]`

`naver_search_collector.py` 를 **`place_search_collector.py` v7.0** 으로 재작성.

v6.0 은 `soup.find_all('a')` 와 `get_text()` 로 페이지의 모든 앵커 텍스트를
이어 붙여 장소명을 만들었다. 정규식 정제가 부족한 게 아니라 **비구조화
HTML 에서 상호명을 추측하려 한 접근 자체**가 원인이었다.

```
'장난감도서관 산본점' + 분류 '장난감대여' -> '군포장난감도서관산본점장난감대여'
'용산구육아종합지원센터와 업무협약 체결'      (뉴스 문장)
'안양에는 무료 박물관과 환경'                (블로그 제목)
```

먼저 네이버 지역검색 API 를 시도했으나 이 앱에 권한이 없다
(`HTTP 401 errorCode 024 Scope Status Invalid`). 카카오 로컬로 전환했다.

| | v6.0 | v7.0 |
|---|---|---|
| 소스 | 네이버 HTML 스크래핑 | 카카오 로컬 API |
| 장소명 | 앵커 이어붙임 + 정규식 | `place_name` 그대로 |
| 좌표 | 전량 공란 | **100%** |
| 중복 제거 | 이름 문자열 | **카카오 장소 고유 id** |
| 이름 길이 평균 | 20~30자(손상) | **7.8자** |

수집: 64개 시군구 x 8개 테마, API 1,614회, 2분 42초, **8,006건**.

데이터 구성은 사용자 판단으로 정했다.
사설 미술학원 테마는 제외했다(추정 2,600건, 전체의 26%인데 정기 교습
상업시설이라 주말 나들이 목적과 성격이 다르다). 계곡은 이름에
`계곡/유원지/휴양림` 이 있는 것만 남겼다(카카오가 계곡으로 분류한
`수리골`·`진골`·`무성골` 등은 시설 없는 미세 지명이다).

## 32) 공공육아 수집기 재작성 `[확인]`

`public_childcare_collector.py` v2.0 을 **v3.0** 으로 재작성. 같은 결함이
남아 있었다. 저장돼 있던 564건이 좌표 0% 이고 이름이 이런 상태였다:

```
2026.07.09.마포구육아종합지원센터직원 채용 공고     <- 채용 공고
구로구육아종합지원센터guroccic.guro.go.kr          <- URL
allthatcompany.com>화성시육아종합지원센터           <- 검색 경로
```

카카오는 이 시설들을 전용 분류로 갖고 있다 (`서울형키즈카페`,
`육아종합지원센터`, `장난감대여`). 결과 **498건 좌표 100%**,
공공키즈카페 좌표 19.2% -> **99.8%**.

분류 판정을 2단으로 나눴다. `아동복지시설` 에는 보육원·그룹홈처럼 나들이
대상이 아닌 시설이 섞여 있어 분류만으로 채택하면 부적절한 시설이 들어온다.
조건부 채택 105건을 실측 검증한 결과 전부 공동육아나눔터·아이러브맘카페·
아이사랑놀이터 등 정상이었고 보육원/그룹홈은 0건이었다.

v2.0 이 전 건에 하드코딩했던
`fee_info = '아동 1,000~3,000원 / 보호자 무료 (공공 가성비)'` 도 제거했다.

## 33) 인천 행정구역 개편 — 옛 구 이름으로 질의하면 0건 `[확인]` 🔴

첫 전량 실행에서 인천 3개 구가 통째로 0건이었다. 카카오 주소 데이터가
새 구 이름을 쓰는데 지역 목록이 옛 이름이어서 지역 검증이 전부 실패했다.

```
인천 중구 / 동구  ->  제물포구 · 영종구
인천 서구        ->  서해구 · 검단구
(미추홀·연수·남동·부평·계양은 그대로. 강화군·옹진군은 목록에 없음)

'인천 중구 공원' 질의 -> 반환 주소가 전부 '인천 제물포구'
```

질의로 실제 사용 이름을 확인해 교체했고 64/64 지역 전부 커버됐다
(7,680 -> 8,006건). **지역 목록을 쓰는 코드는 이 개편을 반영해야 한다.**

## 34) 카카오 로컬 사용 시 반드시 지킬 것 4가지 `[확인]`

1. **지역 필터 필수** — 카카오는 질의 지역 밖 결과도 반환한다. 전량 실행에서
   원본 21,313건 중 **10,105건이 지역 불일치로 탈락**했다. 없으면 엉뚱한
   지역의 장소에 잘못된 `region` 이 붙는다.
2. **분류는 말단만 대조** — `category_name` 전체 경로에는 `여행 > 관광,명소`
   같은 광범위 단어가 있어, 경로 전체에 대조하면 `화장품목장`·`공영주차장`·
   `삼신장례문화체험관` 까지 통과한다.
3. **부분 일치 deny 주의** — deny 목록의 `카페` 가 `키즈카페` 에 걸려
   키즈카페가 전량 사라진 적이 있다. 애매한 단어는 완전 일치로 판정할 것.
4. **질의어에 수식어를 붙이지 말 것** — 카카오는 장소 색인이다.
   `어린이박물관` 0건 / `박물관` 15건, `농장체험` 0건 / `농장` 14건.

### 히트율이 아니라 '반환된 장소가 맞는지'가 기준

카카오 키워드 검색은 엉뚱한 곳도 자신 있게 반환한다.
`정릉천복합문화공간 DDM스케이트파크` -> `청년살이발전소`(성북구, DDM은 동대문).
**반환 `place_name` 이 쿼리와 4글자 이상 겹칠 때만 채택**하면
99.0% -> 98.3% 로 거의 손실 없이 오매칭이 걸러진다.
역지오코딩 독립 검증 17/18 일치.

## 35) 후처리를 파이프라인 단계로 승격 (7단계 -> 11단계) `[확인]`

`generate_total_family_data.py` 는 원본에서 **전체를 재생성**한다. 좌표·중복
보정을 데이터 파일에 직접 적용하면 재생성마다 사라진다 (실제로 한 번 날렸다).

```
1~5 수집기 -> 6 generate -> 7 geocode_event_venues -> 8 dedupe_places --apply
  -> 9 collect_seoul_congestion -> 10 collect_amenities -> 11 enrich
```

`run_web.py` 실행부가 단계별 `args` 를 전달하도록 수정했다. 없으면
`dedupe_places.py` 가 배치에서 dry-run 으로 돌아 아무 일도 하지 않는다.

### 중복 제거에서 행사는 절대 병합하지 않는다

같은 공연장의 다른 공연은 좌표가 같고 이름도 유사하다. 좌표+유사도 규칙을
그대로 적용하면 실제로 다른 공연이 사라진다. dry-run 에서 확인한 오병합:

```
'MAC 모닝 콘서트 #1 / #2 / #3 / #5'        회차별 다른 공연
'체홉 낭독극 [세자매]' vs '[바냐 아저씨]'      다른 작품
'마티네콘서트 #2.엘가' vs '#3.베버'          다른 공연
```

`period == '상시'` 인 장소 행만 대상으로 제한했다. 숫자만 다른 지점명도
별개로 본다 (`자양4동1호점` vs `2호점`). 판정 규칙 8케이스 단위 테스트 통과.

## 36) 혼잡도 — 신규 키 없이 해결 `[확인]`

구 PART 8 에는 "프로젝트에 데이터 소스가 아예 없다. 신규 키 발급 + 도입 필요"
로 적혀 있었으나, **이미 보유한 `seoul_api_key` 로 서울시 실시간 도시데이터
(`citydata_ppltn`)가 바로 호출된다.**

```
어린이대공원  약간 붐빔  추정 4,000~4,500명  0~9세 1.6%  2026-08-23 18:35
```

- `scripts/collect_seoul_congestion.py` (배치 9). 관측지점 **121곳**, 29초.
  API 가 좌표를 주지 않아 카카오로 지오코딩해 캐시한다 (105/121 확보,
  실패 16곳은 `관광특구` 등 광역 구역).
- 혼잡도는 몇 분 단위로 바뀌므로 `total_family_data.csv` 에 박지 않고
  `data/seoul_congestion.csv` 로 분리했다. 수집기만 다시 돌리면 최신값이
  반영되고 본 데이터는 건드리지 않는다.
- 웹앱이 좌표 근접(반경 1km)으로 조인해 **2,258건(20.5%)** 이 실측 혼잡도를
  갖는다. 표기는 '이 장소의 혼잡도' 가 아니라 관측지점명·거리·측정시각을
  함께 낸다. 관측지점이 없으면 값을 만들지 않고 그 사실을 표시한다.

## 37) 편의시설 — 기존 진단이 틀렸고, 소스 한계로 해결 불가 `[확인]`

"커버리지 8% 의 병목은 손상된 장소명" 이라는 진단은 **오류였다.** 이름을 전부
정제한 뒤 재측정하니 오히려 0% 였다. 원인은 **네이버가 주요 문화시설에만
플레이스 카드를 띄우는 것**이다.

| 분류 | 건수 | 카드 보유 수율 |
|---|---:|---:|
| 과학관 | 37 | 58% |
| 박물관 | 454 | 42% |
| 전시관·체험학습장·수목원·주말농장 | 478 | 17% |
| 키즈카페 | 1,147 | 8% |
| **농장,목장 / 공원 / 도시근린공원** | 4,159 | **0%** |

`scripts/collect_amenities.py` (배치 10) 는 0% 분류를 제외하고 카드가 있는
유형만 조회한다. 대상 2,116건 중 **167건(7.9%)** 에서 실제 편의시설을 확보,
166건에 태그를 부여했다. 수집 어휘: 주차 122 · 화장실 100 · 예약 70 ·
단체이용 65 · 무선인터넷 63 · 유아시설(놀이방) 39.

> **최종 편의시설 태그 보유는 166건(1.5%)에 불과하다.** 데이터가 동네 공원과
> 소규모 농장 위주라 조회할 정보 자체가 없다. **커버리지를 크게 올릴 방법은
> 없으며 이것이 소스의 한계다.** 카카오는 편의시설을 제공하지 않는다
> (구 PART 8 은 카카오 활성화로 편의시설이 함께 풀릴 것으로 봤으나 오판이었다).

## 38) 웹앱 수정 `[확인]`

### 커플/싱글/맛집 탭 복구

`data/places.csv` 가 없어 `placesData=[]` -> `mergedData=[]` -> 3개 탭 영구
공백이었다. 이 파일은 `014f01e` 에 있었고 `efef7de` 정리 때 삭제됐으나
app.js 참조는 남아 있었다. git 이력에서 복구했다 (실제 데이터 76건).
`metricsData` 는 선언·사용만 있고 대입이 없었다 (`Promise.all` 에서
구조분해한 `m` 을 그대로 버렸다). 대입을 추가했다.

### 레거시 경로의 허위 표현 제거

PART 7 에서 가족 탭만 고쳐져 있었다.

- `crowd: mt.seoul_crowd_level || 'LOW'` -> `|| 'UNKNOWN'`
  (측정값이 없을 때 '여유'로 단정하던 문제)
- `'실시간 현황'` -> `'현황'`, `'현재 인파 혼잡도'` -> 측정 시각 병기.
  `real_time_metrics.csv` 는 2026-07-26 데이터이고 갱신 스크립트가 없다.

### 정렬 옵션 3개 점검

| 정렬 | 판정 | 처리 |
|---|---|---|
| 인기순 | **정상** — `popularity_score` 1,071건이 네이버 데이터랩 실측값이다 | 유지 |
| 한산한순 | **무작동** — `congestion_score \| 5` 로 전 건 동일했다 | 실측 혼잡도로 교체. 2,258건에 4단계 순서 부여 |
| 무료순 | **무작동** — `fee_info` 에 '무료' 0건 | 근거 데이터가 없으면 칩을 숨긴다 |

`calcRecommendScore` 도 `popularity_score || 50`, `congestion_score || 1` 로
전 건 상수를 더해 아무 역할도 하지 않았다. 혼잡도는 실측값이 있을 때만
감점하고, 없으면 감점하지 않는다.

### `fee_info` 에 가격이 아닌 값이 들어가던 버그 🔴

인터파크 크롤러가 `salesPriceGrade`(가격 **등급 수**, 예: 11)를 `price_info`
로 내보내, 웹앱에 **"이용 요금: 2"** 로 표시되고 있었다. 인터파크 랭킹 API
전체 27개 필드를 확인했으나 실제 가격 필드가 없어, 지어내지 않고
`"상세 페이지 참조"` 로 대체했다. 이상값 127건 -> **0건**.

## 39) 수집 안정성 (구 P1-1, P1-2) `[확인]`

- **수집 0건일 때 덮어쓰기 금지 + `exit 1`** 을 6개 수집기 전부에 적용했다.
  그전에는 네이버가 일시 차단되면 1,643건이 조용히 0건이 되고 admin 에
  SUCCESS 로 표시됐다. 수집 함수가 0건을 반환하도록 강제해 6개 전부
  exit 1 반환 + 대상 파일 해시 불변을 실측 확인했다.
- **`except Exception: pass` 4곳 제거.** 실패한 질의를 기록·출력하고 함수
  종료 시 "n/total개 질의 실패 -> 수집 결과가 불완전합니다" 경고를 남긴다.
- `enrich_total_family_data.py` 캐시 결함 2건을 고쳤다. 카카오 비활성 상태의
  `lat=None` 이 캐시에 굳어 활성화 후에도 좌표가 영구 공란이 되던 문제,
  `save_cache()` 가 루프 종료 시 1회뿐이어서 중단 시 전량 유실되던 문제다.
  자체 지오코딩이 실패해도 수집 단계에서 확보한 좌표를 지우지 않도록 했다.

---

# PART 9. 앞으로 수정해야 할 부분 (2026-08-23 2차 세션 기준 최신)

PART 5~8 에서 처리한 항목은 제외한 **현재 시점의 잔여 과제**다.

## 40) 우선순위 요약

| 순위 | 항목 | 심각도 | 담당 |
|---|---|---|---|
| **P0-1** | API 키 평문 커밋 — `.gitignore` 에 넣었으나 이미 추적 중이라 무효 | 🔴 | **사용자** |
| **P1-1** | `enrich` 실행 시 미확인 홍보 문구가 되살아남 | 🟠 | 코드 |
| **P1-2** | `fee_info` 1.2% — 무료 배지·필터 사실상 무력 | 🟠 | 소스 필요 |
| **P1-3** | 커플/싱글/맛집 탭이 정적 76건에 고정 (갱신 경로 없음) | 🟠 | 설계 |
| **P2-1** | 배포본 admin 수집 기능 404 | 🟠 | 설계 |
| **P2-2** | 스케줄러 부재 (11단계를 사람이 눌러야 함) | 🟠 | 설계 |
| **P3** | 데드코드·문서·캐시 파일 정리 | 🟡 | 코드 |
| **보류** | 편의시설 커버리지 · 티켓링크 3% · 수유실 · 비서울 혼잡도 | 🟠 | 해결책 없음 |

## 41) P0-1. API 키 노출 (가장 급함, 미해결) 🔴

`.gitignore` 에 `scripts/config.json` 을 넣었지만 **이미 git 이 추적 중인
파일이라 무효다.** 공개 저장소(`github.com/luckygirlYoo/ABCchallenges`)에
유효성이 확인된 키가 평문으로 남아 있다.

필요 조치:

1. `git rm --cached scripts/config.json` 으로 추적 해제
2. 노출된 키 재발급 (서울시·네이버·카카오)
3. `.env` + `python-dotenv` 로 이전
4. git 히스토리에서 `config.json` 제거

> 되돌리기 어려운 작업이라 사용자 확인 없이 수행하지 않았다.
> 2차 세션에서 새로 발급한 카카오 키도 커밋에서 제외해 로컬에만 두었다.

## 42) P1 — 코드/설계 수정

### P1-1. `enrich` 가 미확인 홍보 문구를 다시 만든다

현재 `recommend_reason` 은 **0건**이다(재생성 때 사라졌다). 그런데
`generate_recommend_reason()` 의 폴백 4종이 남아 있어, 배치 11단계를 돌리면
11,020건 대부분에 다시 붙는다.

```
🧸 영유아 안심 실내 놀이기구와 부모 쉼터가 잘 갖춰진 키즈 파라다이스
👨‍👧 아빠와 아이가 주말에 부담 없이 특별한 추억을 만들 수 있는 베스트 추천지
```

편의시설이 확인된 경우엔 `(네이버 플레이스 등록 정보)` 라고 근거를 밝히지만
폴백은 카테고리만 보고 쓴 홍보 문구다. 선택지: 1) 폴백 제거 2) 사실 서술로 완화.

### P1-2. `fee_info` 1.2% (127건)

새 수집기가 요금을 지어내지 않아 비웠고, 인터파크 이상값도 제거했다. 결과가
정직해진 대신 앱에서 "이용 요금: 정보 없음" 이 되고 무료 배지·필터가
무력해졌다. 요금 정보를 주는 소스가 필요하다.
`0원` 신호는 티켓링크에만 17건 있으나 전부 가족/어린이 필터에서 걸러진다.

### P1-3. 커플/싱글/맛집 탭의 갱신 경로 부재

탭은 복구했지만 `places.csv` 는 git 이력에서 되살린 2026-07 스냅샷이고,
`events.csv`·`real_time_metrics.csv` 는 **생성 스크립트가 없다.**
갱신 경로가 없어 영구히 76건에 고정된다.

## 43) P2 — 구조 개선

- **배포본 admin 404**: 배포본은 100% 정적이라 `/api/refresh`·`/api/weather`
  가 404 다. admin 수집 UI 를 로컬 전용으로 분리하거나 수집을 GitHub Actions
  cron / 서버리스로 옮겨야 한다.
- **스케줄러 부재**: 11단계 배치를 사람이 눌러야 한다. 도입 시 데이터랩
  일일 1,000회 / 전량 수집 1회당 384회 제약을 반영해야 한다(하루 2회 한계).
  혼잡도 수집(29초)은 자주 돌려야 의미가 있어 별도 주기가 필요하다.

## 44) P3 — 정리

- `weekend_data_pipeline/` 93K 데드코드 (`poi_collector` 부재로 import 실패)
- `web_ver0.1/` 72K 구버전 사본
- `scripts/requirements.txt` 부재 (`requests`, `beautifulsoup4`, `pandas`)
- `README.md` 2줄 — 설치·실행 방법 없음
- `events.csv`·`real_time_metrics.csv` 방치(2026-08-22), 생성 스크립트 없음
- 2차 세션에서 생긴 캐시 3개(`venue_geocode_cache.json`,
  `amenity_cache.json`, `seoul_area_coords.json`)를 커밋했다.
  `.gitignore` 대상으로 볼지 판단이 필요하다 — 커밋하면 재실행이 저렴해지고,
  제외하면 저장소가 가벼워진다.
- 배포 경로 2개 병존 (GitHub Pages + Vercel)

## 45) 보류 — 현재 해결책이 없는 항목

| 항목 | 상황 |
|---|---|
| **편의시설 커버리지** | 37) 에서 실측 확정. 동네 공원·소규모 농장은 네이버에 카드 자체가 없고 카카오는 편의시설을 제공하지 않는다. 166건(1.5%)이 상한이다 |
| **티켓링크 커버리지 3%** | `totalResultCount:1118` 인데 쿼리당 10건 고정. 페이징 파라미터 4종 전부 무효 |
| **수유실·기저귀갈이대** | 네이버 conveniences 어휘에 항목 자체가 없다 |
| **비서울 혼잡도** | 서울시 API 는 서울(+서울대공원) 121곳만 제공한다. 경기·인천은 소스가 없다 |


## 부록 A: API 키 유효성 최종 판정 `[확인]`

| 키 | 실호출 검증 | 판정 |
|---|---|---|
| `kakao_api_key` | 2차 세션에서 **새 키로 교체** 후 정상. 지오코딩·키워드검색 약 3,700회 | ✅ **유효 · 주력 소스** |
| `seoul_api_key` | 문화행사 페이징 3,000건 + 실시간 도시데이터 121지점 | ✅ **유효** |
| `naver_client_id` / `naver_client_secret` | 데이터랩 307회 정상. 단 **지역검색 API 는 권한 없음** (`401 errorCode 024 Scope Status Invalid`) | ⚠️ **부분 유효** |
| `tour_api_key` | HTTP 400 — 사유가 서비스 폐기이므로 키 자체 유효성은 판별 불가 | ⚠️ **판별 불가** |
| `tmap_api_key` | `"YOUR_TMAP_API_KEY"` 플레이스홀더 | ❌ 미설정 |
| `ncp_naver_*` | 미검증 (코드에서 사용처 없음) | — |

> 유효한 키 3종이 git 에 평문 커밋되어 있다. `.gitignore` 에 넣었으나 이미
> 추적 중인 파일이라 무효다. **PART 9 · P0-1 이 미해결 상태로 남아 있다.**
> 2차 세션에서 새로 발급한 카카오 키는 커밋에서 제외해 로컬에만 두었다.

## 부록 B: 항목별 처리 이력 (최초 진단 → 현재)

> 최신 잔여 과제 목록은 **PART 9** 를 볼 것.

| 항목 | 최초 | 현재 상태 |
|---|---|---|
| API 키 평문 커밋 | 🔴 | 🔴 **미해결** (PART 9 · P0-1) — `.gitignore` 추가했으나 추적 해제 필요 |
| `congestion_score`/`popularity_score` 난수 생성 | 🔴 | ✅ 해결 (PART 6-21) |
| 위경도 좌표 오차 / 전량 공란 | 🔴 | ✅ **해결** — 89.2% 확보 (PART 8-30) |
| 장소명 손상 (앵커 이어붙임) | 🔴 | ✅ **해결** — 수집기 2개 재작성 (PART 8-31, 32) |
| `description`/`recommend_reason` 100% 템플릿 | 🔴 | ✅ 해결 (PART 7-25, 26). 단 enrich 폴백 잔존 (PART 9 · P1-1) |
| 혼잡도 실측 소스 부재 | 🟠 | ✅ **해결** — 서울시 실시간, 2,258건 (PART 8-36) |
| 편의시설 커버리지 8% | 🟠 | ⛔ **해결 불가 확정** — 소스 한계, 166건이 상한 (PART 8-37) |
| 다른 수집기의 `except Exception: pass` | 🟠 | ✅ **해결** — 4곳 전부 제거 (PART 8-39) |
| 수집 실패 시 데이터 전멸 | 🔴 | ✅ **해결** — 6개 수집기 전부 (PART 8-39) |
| `app.js` `metricsData` 미대입 + `places.csv` 부재 | 🟠 | ✅ **해결** (PART 8-38) |
| `fee_info` 에 가격 아닌 값 | (미발견) | ✅ **해결** — 이상값 127 → 0건 (PART 8-38) |
| `live_ticket_crawler.py` `timedelta` 미import | 🟠 | ✅ 해결 (PART 6-20) |
| 인터파크 `KIDS`/`FAMILY` 키 불일치 | 🟠 | ✅ 해결 (PART 6-20) |
| enrich 캐시 미작동 | 🟠 | ✅ **해결** — 중간 저장 + 캐시 오염 방지 (PART 8-39) |
| 좌표 기반 중복 미제거 | (미발견) | ✅ **해결** — 파이프라인 단계로 승격 (PART 8-35) |
| 인천 행정구역 개편 미반영 | (미발견) | ✅ **해결** (PART 8-33) |
| 배포본 admin 수집 기능 404 | 🔴 | 🟠 **미해결** (PART 9 · P2-1) |
| 스케줄러 부재 | 🟠 | 🟠 **미해결** (PART 9 · P2-2) |
| `weekend_data_pipeline/` 데드코드 | 🟡 | 🟡 **미해결** (PART 9 · P3) |

## 부록 C: 실행 검증 이력

| 단계 | 목적 | 요청 수 |
|---|---|---:|
| 1차 (PART 2-6) | 미작동 원인 진단 | 12 |
| 2차 (PART 5) | 대체 엔드포인트 탐색 | 약 20 |
| 3차 (PART 5) | dry-run 검증 | 약 30 |
| 4차 (PART 5) | 문화행사 실제 수집 2회 | 약 350 |
| 5차 (PART 5) | 데이터랩 정규화 실증 | 2 |
| 6차 (PART 8) | 카카오 좌표 전량 (2,497건) | 2,497 |
| 7차 (PART 8) | 공연장 venue 조회 | 약 2,100 |
| 8차 (PART 8) | 장소 전량 수집 2회 (인천 개편 전/후) | 3,214 |
| 9차 (PART 8) | 공공육아 전량 수집 | 463 |
| 10차 (PART 8) | 서울시 실시간 도시데이터 | 약 260 |
| 11차 (PART 8) | 편의시설 조회 | 2,116 |

모두 무료 API 이며 금전적 비용은 발생하지 않았다.
카카오 로컬이 주력 소스가 됐으므로 일일 호출 한도를 확인해 둘 것.

---

*본 보고서는 코드 정적 분석 + 데이터 파일 실측 + 사용자 승인 하의 외부 API 실호출로 작성되었다.*
*PART 1~4 는 수정 전 진단, PART 5~8 은 수정 내역, PART 9 는 잔여 과제다.*
